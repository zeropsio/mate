# Verified facts

Every entry names how it was established. A path is a claim someone can re-check in a minute; a
live measurement is a claim that decays. Trust neither kind blindly after a dependency bump.

## Measured against a running system

Carried over from the fork README's live checks (2026-08). Re-measure before relying on any of it.

| Fact                                      | Result                                                   |
| ----------------------------------------- | -------------------------------------------------------- |
| Node in the container                     | v24.16 / v24.18 — the server needs ^24.13                |
| Agents installed                          | `claude` 2.1.241, `codex` 0.139.0                        |
| Headless `claude -p` in the container     | works                                                    |
| `zerops_*` MCP tools from headless claude | works — `zerops_discover` returned the project name      |
| Public HTTPS per container                | HTTP/2, ~86 ms, TLS terminated at the Zerops L7          |
| WebSocket upgrade                         | already in zcp's nginx template                          |
| Zerops API from a browser                 | `access-control-allow-origin: *` — no proxy needed       |
| Server footprint in the container         | ~110 MB (2 GB box; wants 4 GB with an agent running)     |
| White on brand teal `#00b1a3`             | 2.61:1 — below AA, hence the deepened `#007e72` (4.82:1) |

## Measured against the `eval` project (2026-08-27)

Project `eval` `Rd2bffa7R32zxR2q5UeCcA`, its `zcp` service `EXiSBjznSNeZe4S3klsXnA`, over an active
project VPN. Re-runnable with a project-scoped API token.

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | How it was established                                                                                                                               |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The L7 completes a WebSocket upgrade on a non-8080 declared port.** A throwaway `wsprobe` service (Bun, `alpine/bun@1.3.9`) declared ports 8080 and 3773, both `httpSupport: true`, running the same `Bun.serve` echo handler on both. `curl --http1.1` with upgrade headers to `wss://wsprobe-2333-3773.prg1.zerops.app/` returned `HTTP/1.1 101 Switching Protocols`, identical to 8080. A real client (Node's `WebSocket`) completed the handshake in 24–53 ms and echoed a message round-trip in 6 ms on both ports, concurrently — nothing here is 8080-specific.                                                                                                                                                                                                                                                                     | created and deleted `wsprobe` (`Ch2jYZL1Qyqc35aHphOLSQ`) in `eval`; see `questions.md` history, Q-09 removed                                         |
| **Adding a declared port to a running service means redeploying, and redeploying replaces the container.** There is no `PUT`/`PATCH` on `/service-stack/{id}` — `ports`, `requestedPorts`, and `customPortsEnabled` are read-only there. The only way to add a port is to list it under `run.ports` in `zerops.yml` and redeploy (`POST` through `zcli service deploy`, i.e. the same build/deploy pipeline as any code change). Proven on `wsprobe`: redeploying with a third port (4001) changed `GET /service-stack/{id}/container` from container `BCbXggxJRJqVQRCKzxrniA` (`wsprobe-runtime-1`) to a brand-new `sS73clQOSWe92yNiU0B2BQ` (`wsprobe-runtime-2`), and a WebSocket held open across the redeploy died with close code 1006 mid-build, ~33 s in.                                                                             | redeployed `wsprobe` with an extra port while a live WS client polled it every second; diffed `/container` before and after                          |
| **`requestedPorts` is not how a port gets added on a `customPortsEnabled: true` service type, and adding a raw (non-HTTP) TCP port doesn't touch it either.** Declaring a fourth `wsprobe` port with no `httpSupport` (`protocol: tcp` only) landed it directly in `ports` with `scheme: tcp` on the next redeploy; `requestedPorts` stayed `{isActive: false, requestedPorts: []}` throughout every redeploy. `customPortsEnabled` instead tracks the _service type_: `true` on every user runtime we checked (`bun`, `alpine`, `zcp`), `false` on the `core` build-runtime type. `/service-stack/{id}/public-port-routing` (`POST RequestPublicPortRouting`) is a separate mechanism — raw TCP/UDP port-forwarding with a public/internal port pair and a firewall IP allowlist, not HTTP subdomain routing, and unrelated to `run.ports`. | same `wsprobe` redeploys; read `RequestPublicPortRouting`, `BaseRequestedPortsJsonObject`, and the `ResponseServiceStack` schema in the OpenAPI spec |
| **`VSCODE_PASSWORD` is set and readable straight from the API** — `GET /service-stack/{id}/user-data` returns it in `content`, unmasked. The `/user-data/{id}/reveal` endpoint is not needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | listed the service's user-data as a normal token holder                                                                                              |
| **The full auth chain works from an external client.** `GET /` on the public URL 302s to `/zcp-login`; `GET /zcp-auth/<password>` returns `Set-Cookie: __zcp_auth=…; SameSite=None; Secure; Partitioned` and 302s to `/`; that cookie then reaches code-server through the L7.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | curl, no browser involved                                                                                                                            |
| **Non-8080 declared ports are publicly routed.** The `app` service declares only port 3000 and answers on `https://app-2333-3000.prg1.zerops.app`. Both ports carry `scheme: http` and there is no per-port WebSocket setting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | fetched both service objects and the URL                                                                                                             |
| **The service env carries the full container URL.** `zeropsSubdomain` = `https://zcp-2333-8080.prg1.zerops.app` — scheme, host, port and region, not a prefix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | service user-data                                                                                                                                    |
| **The project object carries the region too.** `zeropsSubdomainHost` = `"2333"` (a bare prefix, as documented) but `publicZone` = `fte23prpara6p2koq60b9pvsgk0.prg1-zerops.zone` — the region is derivable from it. Its first label is also the VPN routing domain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `GET /project/{id}`                                                                                                                                  |
| **Services are supervised declaratively via `startCommands` in the service YAML**, not only by imperative `zsc unit create`. zcp's own nginx and code-server are both `startCommands` entries.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `ZEROPS_YAML` in the service env                                                                                                                     |
| **A dynamically created `zsc` unit is a real enabled systemd unit on the rootfs** — `zerops@sshfs-app.service`, `UnitFileState=enabled`, `FragmentPath=/usr/lib/systemd/system/…`, created with `ZCP_SSHFS_HOSTNAMES` unset. Whether it returns after a restart is still open, see `questions.md` Q-01.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | SSH into the container                                                                                                                               |
| The `eval` zcp already runs a z3 server on `127.0.0.1:3773`, bound to loopback — hence the SSH tunnel in `poc/pair.sh`. Container uptime at time of writing was 13 days, so nothing there has been restart-tested.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `ss -ltnp` over SSH                                                                                                                                  |

## Measured against project `p` (2026-08-27)

Project `p` `uHqwF26oTQ6V8D86HeABIA`, a real `zcp@1` service `z3probe` (`Nq49woUlTBeFYdP0jg4DQA`), left running —
see `questions.md` history and the team-lead thread for its credentials. Not `eval`; `EXiSBjznSNeZe4S3klsXnA` was
never written to.

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | How it was established                                                                                                                                                                             |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`ports` is not a field on any service-creation/import schema — declaring it at the top level of an import YAML is silently dropped, not rejected.** `RequestPostServiceStack`, `RequestPostStandardServiceStack`, and `RequestServiceStackImport` (the `yaml` field's shape, matching the public import-YAML reference's "all possible configuration options" example) have no `ports` key anywhere; the only place a port list exists in any schema is nested at `zeropsYaml.run.ports`. Importing `z3probe` (`type: zcp@1`) with a top-level `ports: [8080, 3773]` succeeded with no error, but the resulting service sat at `READY_TO_DEPLOY` with `ports: []` until it was actually deployed.                                                                                                                                                                                                                                                                                                             | `zcli project service-import` with a top-level `ports:` key against project `p`; `GET /service-stack/{id}` before any deploy                                                                       |
| **`zcp@1` can declare a second port exactly like any other `USER`-category runtime — the type carries no platform-side cap.** Read `eval`'s real zcp recipe (`GET /service-stack/{id}/user-data`, `ZEROPS_YAML`, read-only) to get its exact shape — `build.base: ubuntu@22.04`, `run.base: zcp@1`, `run.ports: [{port: 8080, httpSupport: true}]`, the `curl .../install.sh \| sudo sh` + `zcp init` + `zcp init nginx` init sequence, `nginx`/`vscode` `startCommands` — then deployed the identical recipe onto `z3probe` with one added line, `{port: 3773, httpSupport: true}`. It went `ACTIVE` with `ports: [{port: 8080, scheme: http}, {port: 3773, scheme: http}]`, no rejection, no different behavior from a plain runtime.                                                                                                                                                                                                                                                                         | deployed the real `zeropsio/zcp` install recipe (`build.base: ubuntu@22.04`, `run.base: zcp@1`) onto `z3probe` with `run.ports` listing both 8080 and 3773; `GET /service-stack/{id}` after deploy |
| **The 3773 origin routes correctly at the platform edge; it 502s only because nothing in the container listens there yet.** `https://z3probe-2696-8080.prg1.zerops.app/` 302s to `/zcp-login` exactly like a real zcp instance (confirmed further by a working `/zcp-auth/<password>` → `Set-Cookie: __zcp_auth=…` round-trip) — this is a genuinely functional zcp install, not a stub. `https://z3probe-2696-3773.prg1.zerops.app/` answers `502` from nginx, because the recipe's `nginx.conf.tmpl` (see "Read from source" below) only binds 8080; nothing rebinds it for a second port. Getting 3773 to answer is a recipe/software change (start something on it), not a platform blocker — this closes the loop with Q-09's finding that the L7 itself upgrades and routes any declared port equally.                                                                                                                                                                                                    | `curl -sSI` and `curl -sSI .../zcp-auth/<password>` against both `z3probe` origins                                                                                                                 |
| **Each declared port is a separate origin, and therefore a separate cookie jar.** `z3probe`'s two ports resolve as distinct hostnames — `z3probe-2696-8080.prg1.zerops.app` and `z3probe-2696-3773.prg1.zerops.app` — following `{service}-{prefix}-{port}`. This is why z3 on its own port needs no `__zcp_auth` gate: the 8080 cookie cannot apply to it, and z3's pairing → bearer → WS-ticket chain is real auth. Previously asserted by the platform owner; now measured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `curl` against both `z3probe` origins after the two-port deploy                                                                                                                                    |
| **`GET /service-stack/{id}/user-data` responds `{list: [{key, content, type, sensitive, editable, internal, serviceStackName, …}]}`** — a flat array, not a keyed object. `VSCODE_PASSWORD` came back unmasked (`sensitive: false`); `zeropsSubdomain`'s `content` is newline-joined, one URL per declared port (`https://z3probe-2696-8080.…\nhttps://z3probe-2696-3773.…`). Confirms H-01/H-02's assumption about this endpoint's shape, used by `fetchZcpVscodePassword` in `apps/web/src/zerops/api.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `curl -H "Authorization: Bearer <token>" https://api.app-prg1.zerops.io/api/rest/public/service-stack/Nq49woUlTBeFYdP0jg4DQA/user-data`                                                            |
| **Before the H-02 pairing endpoint is deployed on a container, `GET /z3-pair/<password>` and `GET /healthz` both answer `302` to `/zcp-login` on the 8080 origin, not `404`.** `z3probe` (still on its original two-port recipe, no `z3-pair`/`healthz` locations rendered into `nginx.conf`) 302-redirected both paths — nginx's single-`location /` catch-all handles anything undeclared, exactly like a plain unauthenticated hit on `/`. `https://z3probe-2696-3773.prg1.zerops.app/healthz` (the z3 origin, not 8080) still `502`s, unchanged from the row above. `apps/web/src/components/zerops/connect.ts` treats an opaque redirect (`fetch(..., {redirect:"manual"})`) as its own "predates the endpoint" case for exactly this reason, rather than only checking for `404`.                                                                                                                                                                                                                         | `curl -sSo /dev/null -w "%{http_code}"` against `.../z3-pair/<password>` and both origins' `/healthz`, 2026-08-27, ~09:40 UTC                                                                      |
| **The whole browser-only chain works end to end — this is the POC's central claim, and it is now measured.** Against `z3probe` after the container-side deploy, from a laptop with no VPN and no SSH: `/healthz` on 8080 with no cookie → `200 {"initComplete":true,"z3Up":true}`; `/.well-known/t3/environment` on 3773 → `200`, `serverVersion` `0.0.35` (the pinned build); `/z3-pair/<wrong>` → `302` to the login page, minting nothing; `/z3-pair/<correct>` → `200` with `access-control-allow-origin: *` and `{credential, expiresAt, origin}`, a 12-char credential with a ~5 min TTL; RFC 8693 exchange of it at `POST /oauth/token` on 3773 → `200` with scopes `orchestration:read`, `orchestration:operate`, `terminal:operate`, `review:write`, `relay:read`; `POST /api/auth/websocket-ticket` → a 192-char ticket; `GET /ws?wsTicket=…` → **`101 Switching Protocols`** with a valid `Sec-WebSocket-Accept`. This answers the former Q-06 (Connect round-trip), which had never been exercised. | `curl` only, whole chain re-runnable with the zcp service's `VSCODE_PASSWORD`                                                                                                                      |
| **`POST /oauth/token` requires form encoding.** An `application/json` body is rejected with `415 Unsupported Media Type`; `application/x-www-form-urlencoded` succeeds. Anyone scripting the pairing exchange by hand will hit this first.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `curl` against `z3probe`, both encodings                                                                                                                                                           |

### This repo

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Where                                                                                                                                    |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| **The browser brand-checks `fetch` against `Window`.** Storing the global as a class field and calling it as `this.#fetch(...)` throws `Failed to execute 'fetch' on 'Window': Illegal invocation`. React Native tolerates the unbound form, so code lifted from mobile passes its unit tests (which inject a fake fetch), typecheck and lint, then fails for every web user. Bind it: `globalThis.fetch.bind(globalThis)`. Found by driving the real browser, not by any test.                                                                                                                                                                                                                                                                                                  | `packages/client-runtime/src/zerops/api.ts`; regression test in `api.test.ts` emulates the brand check                                   |
| **A zcp container is identified by its service _type_, not its hostname.** `zcli` matches the literal name `zcp` because that is what its own flow creates, but `serviceStackTypeInfo.serviceStackTypeVersionName` is `zcp@1` on every container regardless of hostname, and it already rides on the stack response. Matching by name reports `zcp service is missing` for a project that demonstrably has one. Switching to type matching also revealed that two of this account's projects (`p`, `zleave`) hold **several** `zcp@1` services, which name matching had been silently hiding behind a wrong single-service answer.                                                                                                                                               | `apps/web/src/zerops/candidates.ts` `isZcpService`; observed live in the project list                                                    |
| **Mobile typechecks with `tsc`, not `tsgo`.** `apps/mobile`'s configured `typecheck` script is `tsc --noEmit` and is clean; running `tsgo` there instead reports ~58 spurious `TS2345 … type 'never'` errors on React Navigation `navigate(...)` calls across untouched files. Use each package's own configured checker.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `apps/mobile/package.json`                                                                                                               |
| `t3 serve` has **no base-path flag** — mode, port, host, baseDir, cwd, devUrl, noBrowser, bootstrapFd, autoBootstrap, logWebSocketEvents only (the `--tailscale-serve` flags were removed at the freeze, 2026-08-29). z3 must own an origin root; it cannot be mounted under a path prefix. `cwd` is a trailing positional argument, not a flag; when `--base-dir` is unset it defaults to `~/.t3`; `--mode` is exactly `web` \| `desktop`.                                                                                                                                                                                                                                                                                                                                      | `apps/server/src/cli/config.ts:173-198`, `apps/server/src/os-jank.ts:105-108`, `apps/server/src/config.ts:22-23`                         |
| **Correction, 2026-08-27:** the scriptable, `--json`-capable pairing mint is `t3 auth pairing create`, not `t3 pair` as this row previously said. `t3 pair` (`cli/pair.ts`) _discovers an already-running server_ via its persisted runtime-state file and prints a QR code — no `--json` flag exists on it at all. `t3 auth pairing create` (`cli/auth.ts`'s `authCommand → pairingCommand → pairingCreateCommand`) operates directly on the on-disk DB under `--base-dir`, needs no running server, and with `--json` emits `{id, credential, label?, scopes, expiresAt, pairUrl?}` (`pairUrl` only when `--base-url` is passed). `poc/pair.sh` and `hacks.md` H-02 already used the right command; `map.md`'s "z3 pairing chain" section still names `t3 pair` for this step. | `apps/server/src/cli/auth.ts:59-114`, `apps/server/src/cliAuthFormat.ts:27-67`                                                           |
| An environment is `{id, label, source, target: {httpBaseUrl, wsBaseUrl}}`; a paired remote server is a `BearerConnectionTarget`, no new type needed for a zcp container                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `packages/client-runtime/src/environment/knownEnvironment.ts`, `connection/model.ts:18`                                                  |
| The endpoint contract already models `reachability: "private-network"` and `kind: "private-network"` — the VPN case is expressible upstream                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `packages/contracts/src/remoteAccess.ts`                                                                                                 |
| The Zerops session client (real GUI login — email/password, TOTP, recovery codes, refresh with 401-coalescing — plus a non-expiring Integration Token as an alternative credential) is shared code now; H-04 is paid back. Mobile backs it with SecureStore, web with `localStorage`; a mid-2FA partial credential stays deliberately memory-only on both.                                                                                                                                                                                                                                                                                                                                                                                                                       | `packages/client-runtime/src/zerops/`                                                                                                    |
| **2026-08-27:** `ZeropsSignIn` is now mounted in `ZeropsSettings.tsx` — signed out (or mid-2FA) it's the only content; signed in it renders nothing (by design) and `ZeropsProjectPicker` takes over. The old bare token gate is gone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `apps/web/src/components/settings/ZeropsSettings.tsx`                                                                                    |
| A post-sign-in sheet that lists environments and connects them already exists, built for T3 Connect's Clerk auth                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `apps/mobile/src/features/cloud/ConnectOnboardingRouteScreen.tsx`                                                                        |
| Web first-run injection point                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `apps/web/src/routes/_chat.index.tsx` — `HostedStaticOnboardingState`                                                                    |
| Only Electron main can spawn a local binary; `npx t3 serve` and hosted web have no such seam by design                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `apps/desktop/src/ssh/DesktopSshEnvironment.ts`                                                                                          |
| **2026-08-27:** `filesystem.browse` (the atom `apps/web/src/state/filesystem.ts` wraps) already filters to directories only and needs no cwd for an absolute `partialPath` — calling it with `partialPath: "/var/www/"` lists exactly the directory entries under `/var/www`, `{name, fullPath}` each, no file/dir flag on the wire because everything returned already is a directory. No new RPC needed for the `/var/www` mounts panel.                                                                                                                                                                                                                                                                                                                                       | `apps/server/src/workspace/WorkspaceEntries.ts:191-238` (`dirent.isDirectory()` filter), `packages/contracts/src/filesystem.ts`          |
| **2026-08-27:** a project's `workspaceRoot` is on `OrchestrationProjectShell` and reachable per-environment via `useProjects().filter(p => p.environmentId === id)` — the connected environment's own workspace root is derivable client-side with no new request and no hardcoded path guess.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `packages/contracts/src/orchestration.ts:445-458`, `apps/web/src/state/entities.ts` (`useProjects`)                                      |
| **2026-08-27:** `useZeropsCandidates()` only ever marks a candidate `"connected"` (and sets `environmentId`) by matching its derived z3 origin against an environment `useEnvironments()` already has (`environment.displayUrl`) — so a "connected" candidate cannot exist while `useEnvironments()` returns zero environments. In `_chat.index.tsx`, `HostedStaticOnboardingState` only renders when `environments.length === 0`, so the "exactly one connected project → auto-navigate" landing branch cannot fire from pre-existing state in that mount; it's exercised instead by the picker's `onConnected` callback resolving the same environment the instant a connect action completes.                                                                                 | `apps/web/src/zerops/candidates.ts:87-129` (`deriveZeropsCandidate`), `apps/web/src/components/zerops/landing/ZeropsSignedInLanding.tsx` |

### zcp

Read every `Where` in this section against `../zcp` on branch `feat/z3-container` — the z3 paths
do not exist on `main` (`README.md`, "The `zcp` half is parked on a branch"). Two rows also cite
`plans/research/zcp-install-delivery-architecture-2026-08-24.md`, which is an uncommitted working
file in `zcp` on `main`, on neither branch's history.

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Where                                                                                                                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| nginx listens on 8080 with exactly one `location /`, proxying to code-server on 127.0.0.1:8081; WebSocket upgrade and a 86400s read timeout are already in it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `internal/content/templates/nginx.conf.tmpl`                                                                                                                            |
| The public-URL gate is a cookie whose value is the raw `VSCODE_PASSWORD`; `GET /zcp-auth/<password>` sets it. **If the env var is unset there is no auth at all.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `internal/init/init_nginx.go:54`, template lines 18-24 / 89-103                                                                                                         |
| `zcp init` runs as `run.initCommands`, so it re-runs on every container start and re-renders nginx.conf                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `internal/init/init.go:56`, `plans/research/zcp-install-delivery-architecture-2026-08-24.md`                                                                            |
| Long-lived processes are supervised by systemd through `sudo -E zsc unit create <name> "<cmd>"` — the same primitive the sshfs mounts use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `internal/init/init_sshfs.go:52`, `internal/platform/mounter.go:83`                                                                                                     |
| Sibling services are sshfs-mounted at `/var/www/<hostname>` from that service's own `/var/www`, with no reserved-name list anywhere in the mount code — any sibling hostname can claim a path under `/var/www` (hacks.md H-21). Confirmed live: project `p` (home to `z3probe`) has a sibling literally named `app` — the reason z3's own workspace moved to `/home/zerops/app`, outside `/var/www` entirely.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `internal/ops/mount.go:13`, `internal/init/init_sshfs.go`; `zcli service list --projectId uHqwF26oTQ6V8D86HeABIA`                                                       |
| Mount state is computed live from `/proc/mounts` + readdir + `systemctl`; **no manifest file exists**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `internal/ops/mount.go:260`                                                                                                                                             |
| `zcp@1`'s live `initCommands`/`startCommands` list is genuinely outside this repo — no file here is what a running container actually boots from, matching README's "platform-owned" claim. A checked-in MIRROR now exists at `deploy/zcp-container.yml` (adds z3/z3sidecar), reviewable but not itself deployed by anything — someone applies it on the platform side. It runs in two phases: every `initCommands` entry finishes (`curl install.sh \| sudo sh`, `zcp init`, `sudo -E zcp init nginx`, …) BEFORE any `startCommands` process (`zerops@nginx`, `@vscode`, …) launches — so nginx answering a request at all already proves that boot's `zcp init` reached completion (a required-step failure aborts the whole container start). This is why `/healthz` (hacks.md H-07) is only interesting for what happens AFTER that point, not as an "is the container up yet" signal.                           | boot journal table in `plans/research/zcp-install-delivery-architecture-2026-08-24.md` §1.1; `verified.md`'s own `z3probe` deploy row above; `deploy/zcp-container.yml` |
| `zcp service start <name>` (`internal/service/service.go`) is a package-level `map[string]execConfig` — the literal `ExecStart` every `startCommands`-declared long-lived process (`nginx`, `vscode`, and now `z3`/`z3sidecar`) runs, with signal forwarding and optional `systemctl set-property --runtime <unit> TasksMax=…` tuning shared across every entry. Distinct from — and layered on top of — the `sudo -E zsc unit create <name> "<cmd>"` primitive sshfs mounts use to get a unit registered in the first place; z3/z3sidecar do NOT use that primitive (hacks.md H-05, paid back) — `deploy/zcp-container.yml`'s `startCommands` registers their units the same way nginx/vscode already are. `z3sidecar`'s exec config self-execs: `binary: "zcp"`, `args: ["zcp", "z3sidecar"]` — `exec.LookPath("zcp")` resolves the same binary already running as this process's parent, same as any other entry. | `internal/service/service.go`                                                                                                                                           |
| **A `startCommands` entry is a structured object, not a bare string** — `{name: string, command: string (required), workingDir?: string, initCommands?: string[]}`. Pushing a plain string list (`startCommands: [nginx, vscode]`, as an earlier abbreviated read of `ZEROPS_YAML` suggested) fails schema validation: `yaml: unmarshal errors: line N: cannot unmarshal !!str "nginx" into zeropsYamlParser.startCommand`. The real value behind a bare name is `{name: nginx, command: "zcp service start nginx"}` — confirmed by getting this exact error from `zcli service push` against `z3probe`, then fixing it and deploying successfully, 2026-08-27.                                                                                                                                                                                                                                                      | `internal/schema/testdata/zerops_yml_schema.json` → `properties.zerops.items.properties.run.properties.startCommands.items`                                             |
| **z3/z3sidecar supervised via `startCommands` survive a genuine service restart with zero manual steps** — `zcli service stop z3probe && zcli service start z3probe`, then polled `/healthz`: `z3Up` went `false` → `true` within ~10s of the container reappearing (`initAt` timestamp confirmed a fresh boot, not a cached response), no SSH, no re-running any init command by hand. A fresh mint (H-02) and the full pairing → bearer → ws-ticket → `wss://…/ws` 101 chain (see the row above) both completed again immediately after.                                                                                                                                                                                                                                                                                                                                                                           | `zcli service stop`/`start` against `z3probe`, then `curl` polling + the same chain as the row above, 2026-08-27                                                        |

### zcli

| Fact                                                                                                                                                                                                     | Where                                                                  |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `zcli vpn mode single\|multi` is fully implemented, with a planner, a crash-recovery journal and a file lock — not scaffolding                                                                           | `src/cmd/vpnMode.go`, `src/vpnManager/`                                |
| Multi mode means one WireGuard interface **per project**, named `zp<13 chars of base32(sha256(projectID))>` — not one interface with several peers                                                       | `src/vpnManager/tunnel.go:23`                                          |
| The tunnel is `wg-quick up <conf>`, shelled out with no sudo prefix, so the whole `zcli vpn up` process must be run as root. `wgctrl` is used only for key generation.                                   | `src/wg/wgquick_cmd.go:11`, `src/wg/darwin.go`                         |
| Root is needed for exactly three things: creating the utun, setting routes, and writing `/etc/resolver/*` (the latter is baked into the config's `PostUp`)                                               | `src/wg/darwin.go:200-218`                                             |
| `zcli vpn status --json` emits a stable schema with machine-readable reason codes, translated to prose only at the cmd layer                                                                             | `src/cmd/vpnStatus.go`                                                 |
| `vpnManager.Coordinator` is a renderer-agnostic Go library with two public methods, `Inspect()` and `Apply()` — `zcli-interactive` already drives it from a Bubble Tea TUI in-process with zero IPC      | `src/vpnManager/coordinator.go`; `../zcli-interactive/src/vpnControl/` |
| `zcli zcp [project] [agent]` already does the whole terminal-side flow: resolve project → find the zcp service → connect VPN → SSH → launch an agent, optionally in a persistent tmux                    | `src/cmd/zcp.go`, `src/zcpSession/`                                    |
| `Candidate{Project, Service, Agents, Reason}` is the existing model for "which projects are usable and why not" — reasons cover project inactive, no zcp service, several zcp services, service inactive | `src/zcpSession/target.go:24`, `catalog.go:171`                        |
| `docs/multi-vpn-architecture.md` on branch `kh-zcp-multi-vpn` is an approved implementation spec — read it before touching VPN                                                                           | `../zcli` on that branch                                               |

### Zerops API

| Fact                                                                                                                                                                                                                | Where                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| Session auth is `POST /auth/login` → `{auth: TokenData, user}`; TOTP and recovery codes both go to `POST /2fa/totp/login` in the same `token` field; refresh is `POST /auth/refresh` with `{refreshTokenId}`        | `../frontend-legacy/libs/zef/src/auth/auth.api.ts`                              |
| Integration Tokens (what `zcli login` consumes) are a separate, non-expiring, org-scoped credential — no refresh                                                                                                    | `../frontend-legacy/libs/models/src/integration-token/`                         |
| `/user/info` has **no top-level `clientId`**; membership is in `clientUserList` and a user can belong to several orgs, so a single org can look like an empty account                                               | `apps/mobile/src/features/zerops/zerops-api.ts`                                 |
| Projects are `POST /project/search` with a `clientId` eq filter; services are `GET /project/{id}/service-stack`                                                                                                     | same                                                                            |
| VPN is plain REST: `POST /project/{id}/vpn` with your own public key returns the project identity, your assigned IP, **and pre-rendered WireGuard config per platform**. Nothing in the handshake is zcli-specific. | Zerops Go SDK `sdk/PostProjectVpn.go`; call site `../zcli/src/cmd/vpnUp.go:270` |
| `connectedStacks` comes back empty on every service — there is no dependency topology to draw                                                                                                                       | fork README                                                                     |

## Measured against the `z3-eval` project (2026-08-28)

Project `z3-eval` `nTV3oMB2SS634ImDJnQckg`, its `zcp` service `gt7tJZjDSk2zyH5XvNeAQQ` — a pre-created `zcp@1` in the shape a fresh pool project has, over the project VPN. The dev loop is `../zcp/eval/scripts/z3-dev-push.sh` (hacks.md H-23).

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | How it was established                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| **Container baseline**: zcp `v9.152.0` (release), Node `v24.20.0`, npm `11.19.0`; Claude logged in (`~/.claude/.credentials.json` present), Codex not (`~/.codex/auth.json` absent); `~/.zcp/` exists (only `serve.log`), `~/.t3` does not.                                                                                                                                                                                                                                                                                                                                                                              | `ssh zcp` after `zcli vpn up nTV3oMB2SS634ImDJnQckg` — `zcp version`, `node -v`, `npm -v`, `ls`                                 |
| **A hand-pushed zcp binary takes over cleanly**: `make linux-amd` → scp → `sudo install` → sha256 match → `cd /var/www && zcp init` (all steps green, extensions reinstalled) → `sudo -E zcp init nginx` → `nginx -s reload` rc=0. `zcp version` afterwards reports the local commit (`v9.152.0-14-g2c677658`).                                                                                                                                                                                                                                                                                                          | `z3-dev-push.sh zcp`, then `zcp version` on the container                                                                       |
| **nginx runs as `zerops`** (master pid owned by zerops, `zcp service start nginx` → `nginx -g 'daemon off;'`, pid file `/tmp/nginx.pid`), so `nginx -t` / `nginx -s reload` work without sudo; `sudo nginx -t` fails on the pid file — do not sudo it.                                                                                                                                                                                                                                                                                                                                                                   | `ps -eo user,pid,args`, `nginx -t`, `nginx -s reload` as zerops                                                                 |
| **Supervision primitives**: `zsc unit` has only `create` and `remove`; units land as `zerops@<name>.service` (`zerops@nginx`, `zerops@vscode`, `zerops@zerops` present; sshfs mounts use the same `zsc unit create sshfs-<host> …`). Restart = `sudo systemctl restart zerops@<name>` (passwordless sudo works).                                                                                                                                                                                                                                                                                                         | `zsc unit --help`, `systemctl list-units --type=service --all`, `sudo -n systemctl status zerops@nginx`                         |
| **The fork packs and installs on the container's Node.** `vp run --filter @t3tools/web build` 11.7 s; `cli.ts build` (tsdown + client copy); new `cli.ts pack` (the publish rewrite → tarball, 0 `catalog:` refs left, 786 `dist/client/` files) → `t3-0.0.33-dev.<sha>.tgz` 18 MB. On the container `npm install <tgz>` into prefix `/home/zerops/.zcp/z3`: 198 packages in 55 s, 282 MB on disk, `node_modules/.bin/t3 -> ../t3/dist/bin.mjs`; `t3 --version` → `t3 v0.0.33` (the dev tag is visible only via `npm ls t3`), `t3 serve --help` renders. Native deps (node-pty) installed without a toolchain complaint. | `z3-dev-push.sh z3` log; `tar -xzOf … package/package.json`; `ls`, `du -sh`, `t3 --version`, `t3 serve --help` on the container |
| **`/healthz` on 8080 answers `302`** (the code-server cookie gate) on a stock nginx config — the S2 location has to sit outside the gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8080/healthz` on the container                                         |
| **The product branch `z3` (cut from `upstream/main` `4c51b4c9b`, T3 0.0.35 + 4 days) packs and runs on the container's Node.** `vp install --frozen-lockfile` (6 s; the POC-era `node_modules` lacked upstream's new `heic-to` and the web build failed on an unresolved import — a rebase always needs the reinstall, now part of the push script) → web build → `cli.ts build` → `cli.ts pack` → `t3-0.0.35-dev.<sha>.tgz` 20 MB → `npm install` into `/home/zerops/.zcp/z3` → `npm ls` `t3@0.0.35-dev.c952342cb`, `t3 --version` → `t3 v0.0.35`.                                                                      | `../zcp/eval/scripts/z3-dev-push.sh z3` from branch `z3`, 2026-08-28                                                            |

## S0 — measured 2026-08-28 (`../zcp/plans/z3-brief-2026-08-28.md` §6)

Every row names the brief's S0 number it answers. API base `https://api.app-prg1.zerops.io/api/rest/public`; `$TOKEN` = a user's access token, never printed. Laptop-only rows need no VPN.

### S0.1 — membership proof (Zerops API, two accounts: the owner = org KRLS `BkC8AGjFQMyFrLbzjHoE9g`, role OWNER; a non-member with its own org)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | How it was established                                                                                                                                                                                                                                   |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`GET /project/{id}` with the caller's token is the membership check, three-way on the status code alone**: member → `200` (body carries `clientId`, `status`, `publicZone`, `name`); valid non-member → `403` code `insufficientPermissions`; bad/expired token → `401` code `notAuthorized`. 30–40 ms each way (5× each). A made-up project id with a member token → `400` code `projectNotFound`, so "forbidden" and "does not exist" never blur.                                                                                                                                                           | `curl -s -w '%{http_code} %{time_total}' -H "Authorization: Bearer $TOKEN" $BASE/project/nTV3oMB2SS634ImDJnQckg` with each token; 2026-08-28                                                                                                             |
| `GET /project/{id}/service-stack` and `GET /service-stack/{id}` follow the same pattern (member `200`, non-member `403 insufficientPermissions`, ~30 ms; the non-member fails fast, no extra lookup cost).                                                                                                                                                                                                                                                                                                                                                                                                      | same curl shape against `/project/{id}/service-stack` and `/service-stack/gt7tJZjDSk2zyH5XvNeAQQ`                                                                                                                                                        |
| **`GET /user/info` has no top-level `clientId`** (confirms the code-read row above); `clientUserList[]` entries are `{id (= clientUserId), clientId, userId, status, roleCode, client:{id, accountName, avatar}, user:{…}}`. The only `roleCode` seen live is `OWNER`; the vendored zerops-go SDK's `ClientUserRoleCodeEnum` declares only `MANAGER` — the SDK enum is stale, never trust it for the role vocabulary. A user can sit in several orgs (the owner has two), so match the PROJECT's `clientId` against the caller's list, never "has any org".                                                     | `curl -H "Authorization: Bearer $TOKEN" $BASE/user/info` for both tokens (member: KRLS + a second org; non-member: exactly one org, zero overlap), ~37 ms                                                                                                |
| Role resolution is NOT free with the project GET: the role comes from `/user/info` → the `clientUserList` entry whose `clientId` equals the project's; a non-owner member can additionally be project-scoped via `GET/PUT /client-user/{clientUserId}/roles` → `{projectRoleList: […]}` (the mechanism zcp's `internal/platform/project_admin.go` `GrantSelfRole` uses). An org OWNER shows `projectRoleList: []` and still has blanket access, so an empty list means nothing on its own — `GET /project/{id}` `200` stays the ground truth. Reading another org's clientUser → `403 insufficientPermissions`. | `curl -H "Authorization: Bearer $TOKEN" $BASE/client-user/<ownClientUserId>/roles` for both accounts; cross-org id with the non-member token                                                                                                             |
| The search endpoints are membership-gated hard, not empty-result: `POST /project/search` and `POST /client-user/search` (body `{"search":[{"name":"clientId","operator":"eq","value":"<clientId>"}]}`, the `EsFilter` shape) with a clientId the caller does not belong to → `400` code `invalidUserInputWithText` "search is not allowed, insufficient privileges". So they cannot be used to probe, and there is no `/project/{id}/user` member-list endpoint anywhere (SDK + GUI grep).                                                                                                                      | `curl -X POST -H "Authorization: Bearer $NONMEMBER_TOKEN" -H 'Content-Type: application/json' $BASE/project/search -d '{"search":[{"name":"clientId","operator":"eq","value":"BkC8AGjFQMyFrLbzjHoE9g"}]}'`; member on own org: `200`, 19 projects, 57 ms |
| Token kinds behave identically for this check: the non-member's stored token and a fresh session token from `POST /auth/login` `{"email","password"}` (no TOTP demanded on that account; response `{auth:{accessToken, refreshToken, expiresIn: 432000, tokenType, userId, …}, user}`, 0.47 s) gave byte-identical `/user/info` and `403` on the project GET. Both are 66-char three-segment strings; the segment lengths differ per token (not decoded).                                                                                                                                                       | `curl -X POST $BASE/auth/login -d '{"email":"…","password":"…"}'` then the calls above with `auth.accessToken`                                                                                                                                           |

### S0.6 — container Node + fork tarball size (answered by the `z3-eval` rows above; the distribution channel itself waits for the release gate, brief §4a)

| Fact                                                                                                                                                                                                                                                                                                             | How it was established                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Container Node `v24.20.0`, npm `11.19.0` (server needs `^24.13`); the product branch's `cli.ts pack` tarball is **20 MB** (`t3-0.0.35-dev.0e517b5b8.tgz`, re-measured today by the push loop), 282 MB installed under `/home/zerops/.zcp/z3` with native deps (node-pty) building without a toolchain complaint. | rows "Container baseline" and "The fork packs and installs…" in the `z3-eval` section; `../zcp/eval/scripts/z3-dev-push.sh all` log, 2026-08-28 (`==> Tarball: … 20M`) |

### S0.7 — registration API + pool claim (code-read of `../frontend-legacy` at `c88b8fefb` on `kh-zcp-pool-exhausted-fallback` and the public spec `../zcp-openapi-2026-08-24.yml`; one non-mutating live probe; no account created)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Where / how                                                                                                                                                               |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Registration is `POST /registration`**, body `RequestUserRegistration`: required `email`, `languageId`, `accountName`, `name`; optional `password` (required unless passkey), `token` (Cloudflare Turnstile), `passkey`, `promoCode`, `reference`. The GUI additionally sends **`claimZcpPool: true`** (only when the route carries `?zcp=true`) and `deviceName` — both absent from the spec, accepted by the backend. Response `ResponseAuthRegistration` = `{auth:{accessToken, refreshToken, expiresIn, twoFAMethods, sudoUntil}, user, webAuthn}` plus undocumented **`zcpClaimed?: boolean`** — absent or `true` = a pool project was claimed (or the signup was not pool-aware), `false` = pool exhausted. The session token comes back immediately.                                                              | spec L6682-6716 / L6957-6970; `registration.model.ts:8-18`, `registration.page.ts:109-111`, `libs/zef/src/auth/auth.model.ts:63-71`                                       |
| **No e-mail-verification gate before login**: `GET /user/verify-email/{id}` finalises the mailed token and `PUT /user/{id}/send-verification-email` re-sends, but nothing blocks `/auth/login` on an unverified address. **TOTP is optional and post-signup only** (`/2fa/totp/registration/start`+`/finish` under a Bearer session; `/2fa/totp/login` only once enabled). No terms/consent field exists on the request.                                                                                                                                                                                                                                                                                                                                                                                                   | spec L20296, L20008, L9975; GUI registration module                                                                                                                       |
| **The captcha is not a required field at the API's validation layer.** Live: `POST /registration` with body `{}` → `400` `invalidUserInput`, metadata names only `accountName`, `email`, `languageId`, `name` as `field is required` — no `token` entry. Response carries `access-control-allow-origin: *`. Whether a missing/invalid Turnstile token is rejected later (score / rate-limit) is NOT determined — see `questions.md` Q-10.                                                                                                                                                                                                                                                                                                                                                                                  | `curl -sS -i -X POST $BASE/registration -H 'Content-Type: application/json' -d '{}'`, 2026-08-28                                                                          |
| The Turnstile site key the GUI uses is `environment.turnstileToken` (`0x4AAAAAABkfI4SNvJav8428` in `environment.local.ts:33`); whether Cloudflare binds it to the GUI's hostnames is not visible from any repo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `registration-form.component.ts:6,17,25`                                                                                                                                  |
| **After `zcpClaimed` the GUI never polls REST**: it waits for the control-plane stack in a WebSocket-pushed service-stack subscription (`listSubscribe`), 30 s timeout, then for the `stack.build` process and subdomain settlement over the same push streams.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `service-stack-base.effect.ts:158`, `zcp-pool-claim-base.effect.ts:226-283` (`#resolveAndEnterSelection$`), `#awaitZcpBuildOutcome$`, `#awaitWorkspaceReachable$`         |
| **Pool-exhausted fallback = two REST calls**: `POST /client/{clientId}/project` (`RequestPostProject`) when the account has no project, else the first project is reused; then **`PUT /project/{id}/first-class-recipe/development-container`** with `{serviceImportYaml, recipeSource: "zeropsio/zcp", createIntegrationToken: true}` (`RequestFirstClassRecipeDevelopmentContainer`, required `serviceImportYaml`). A dedicated first-class-recipe route, not a generic `service-stack/import`. The exact `serviceImportYaml` the GUI generates (`ZeropsYamlBuilder.generate()`) was not traced — needed before S4 runs this for real.                                                                                                                                                                                   | spec L13030-13040, L5446-5463; `zcp-pool-claim-base.effect.ts:431-476`, `:704-753`, `:661-668`; `service-stack-base.api.ts:161-178`; `control-plane-dialog.constant.ts:7` |
| **GitHub OAuth signup**: `GET /github/auth-url?action=REGISTRATION&redirectUrl=<fixed callback>&nonHaRecipe=&haRecipe=&email=` → callback page parses `code`+`state` → `POST /github/user-registration {code, state, email?}` → a `ResponseAuthLogin` shape that may carry `zcpClaimed`. **The pool claim on this path is unimplemented**: `github-auth.page.ts:100-111` says the claim rides in the OAuth `state` as `zcp:true`, but no producer attaches it anywhere (`external-auth-buttons.container.ts:57-67` → `github.effect.ts:35-48` → `github.service.ts:23-37` pass only `authMode`/recipe ids; repo-wide grep finds no other call site). The callback URL is a fixed value registered on Zerops' own GitHub OAuth App — a third-party origin cannot add its own redirect without a platform-side registration. | spec L5521-5532; the cited GUI files                                                                                                                                      |

### S0.12 — envelope extraction from the provider stream (live `claude -p` on `z3-eval`'s zcp + code read of `../zcp` and the fork; Codex code-read only, not logged in)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | How it was established                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The `StateEnvelope` never crosses the wire.** `zerops_workflow action="status"` (the only lifecycle-status carrier) computes the envelope and returns `textResult(RenderStatus(…))` — markdown prose (`## Status / Phase: … / Services: … / Guidance: … / Next:`); the handler's structured-output return (`any`, the Go MCP SDK's `structuredContent` slot) is `nil`, and `structuredContent` appears nowhere in `internal/`. `workflow.Response{Envelope…}` (`json:"envelope"`) is consumed only by `RenderStatus`. The brief's §3 line "StateEnvelope: JSON attached to every workflow-aware tool result" describes an intent, not the code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `../zcp/internal/tools/workflow.go:906-941`, `internal/tools/workflow_develop.go:271-294`, `internal/workflow/render.go:15-26`, `internal/tools/convert.go:106-120`; `grep -rni structuredcontent internal/` → 0 hits (zcp `64f63ae8`) |
| Live, Claude Code stream-json: a `status` call's `tool_result` is one `{type:"text", text}` block of **8 839 bytes of markdown** (804 bytes of state header, the rest atom guidance); `jq -e .` fails on it, zero occurrences of `envelope`. `zerops_discover`'s result IS verbatim JSON (1 592 bytes, parses; keys `project`, `services[]`, `warnings[]` — a narrower shape, no phase/workSession/closeMode). Tool results arrive as arrays of text blocks, never bare strings; no truncation or double escaping. Whole turn: 8–9 events, 12.9 KB (discover) / 26.8 KB (status).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `claude -p '…zerops_workflow action="status"…' --output-format stream-json --verbose --allowedTools mcp__zerops__zerops_workflow` from `/var/www` on the container; `jq` over the jsonl, 2026-08-28                                    |
| The two `launch`/`export` "envelopes" in `internal/tools/launch_status_recovery.go:18` and `workflow_export.go` are bespoke JSON shapes returned via `jsonResult`, not `workflow.StateEnvelope`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | code read                                                                                                                                                                                                                              |
| **Fork seams — MCP tool results reach one provider-agnostic bus unmodified.** Claude: `ClaudeAdapter.ts:1727` runs the CLI through `@anthropic-ai/claude-agent-sdk` `query()` with `settingSources: ["user","project","local"]` (`:1243-1247`, why the container's `~/.claude.json` `mcpServers.zerops` is picked up); `handleUserMessage` (`:2740-2830`) emits `item.completed` with `payload.data = {toolName, input, result: <raw tool_result block>}` — no cap, no stringify. Codex: the driver spawns the persistent `codex app-server` (`CodexSessionRuntime.ts:1185-1204`, JSON-RPC via `effect-codex-app-server`), not `codex exec --json`; `mapItemLifecycle` (`CodexAdapter.ts:466-501`) emits `item.completed` with the raw `V2ItemCompletedNotification` whose `mcpToolCall` item is `{server, tool, arguments, result?: {content: unknown[], structuredContent?, _meta?}, error?, status}` (`schema.gen.ts:20437-20445`). Both map to `itemType: "mcp_tool_call"` (Claude by the `mcp__` name substring, `ClaudeAdapter.ts:736-771`; Codex natively, `CodexAdapter.ts:221-238`). Single hook for a reducer: `ProviderService.ts:234` `runtimeEventPubSub` / `publishRuntimeEvent` (`:286-293`). | `../z3/apps/server/src/provider/Layers/{ClaudeAdapter,CodexAdapter,CodexSessionRuntime,ProviderService}.ts`, `packages/effect-codex-app-server/src/_generated/schema.gen.ts` at `0e517b5b8`                                            |
| Codex CLI in the container: `codex-cli 0.150.1`; `codex exec --json` prints JSONL events, `-o/--output-last-message <file>` the final message — relevant only for headless scripts, not for the fork (which uses app-server). Not logged in (`~/.codex/auth.json` absent), no live run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `codex --version`, `codex exec --help` on the container                                                                                                                                                                                |

### S0.11 — `zcp studio watch` / `topology` (live on `z3-eval`'s zcp, dev build `64f63ae8`, throwaway `s0watch1` = `valkey:single@7.2` NON_HA)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | How it was established                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`zcp studio watch` is a doorbell, not a data feed.** `Event` is `{"type": …}` with exactly three values `connected`, `topology-changed`, `disconnected` — no id, hostname, status or process field. It subscribes over the platform websocket (`POST /web-socket/login` → `wss://<host>/api/rest/public/web-socket/<receiverId>/<token>` → `POST /service-stack/search` with `subscriptionName=ServiceStack__list-subscription`, `wsOutputType: listStream`) and fires on any matching `type:"search"` push without parsing its `add`/`delete` payload. It never re-reads topology itself.                                                              | `../zcp/internal/dataconsole/watch/watch.go:1-30, 53-67, 188-211, 223-284`; live raw events `{"type":"connected"}` then `{"type":"topology-changed"}`      |
| **It fires only on list-membership changes.** Import of `s0watch1` → `topology-changed` at **+0.55 s** after the `zcli` call started (before zcli returned at +16.2 s); delete → `topology-changed` at **+10.0 s** (when the row vanished). `zcli service stop` + `start` on the same service (ACTIVE → STOPPING → … → ACTIVE, ~80 s) produced **zero** events. Foreign add/delete events from other subagents' services arrived the same way — unattributable from the event itself.                                                                                                                                                                     | `(sleep 1800 \| zcp studio watch)` from `/var/www` on the container with per-line `date +%s.%N`; zcli import/stop/start/delete with timestamps, 2026-08-28 |
| **`zcp studio topology` is a direct read (no ES lag)**: `ops.Discover` → `client.ListServicesDirect` (`GET /project/{id}/service-stack`) → `FillSubdomainURLs` → `EnrichWithMetaStatus`. Polled every 1 s it showed `s0watch1` `CREATING` at **+1.05 s**, `ACTIVE` at **+16.4 s** (valkey, no build), `STOPPING` at +1.7 s after stop, `DELETING` at +3.0 s and gone at +9.4 s after delete. One call = 0.26 s real for 5 services. Per service it carries `hostname`, `serviceId`, `type`, `status`, `adoptionState`, `isInfrastructure`, `subdomainEnabled`/`subdomainUrl` — no live process (build/deploy) state; that is `ops.ProjectActivity`'s job. | `../zcp/cmd/zcp/studio.go:123-149`, `internal/ops/discover.go:164`; the 1 s poll loop on the container                                                     |
| No disconnect/reconnect in a 6-minute session; stderr empty. On this container `ZCP_API_KEY` + `ZEROPS_ClientId` sit in the process environment (there is no `/var/www/.mcp.json`); `auth.ResolveCredentials()` reads env first.                                                                                                                                                                                                                                                                                                                                                                                                                          | `/tmp/s0watch.err`; `../zcp/internal/auth/auth.go:121`                                                                                                     |

### S0.8 — agent auth on a second `zcp@1` (`zcpb` `1IM7ENXpTxuLo0uRDXE20w` in `z3-eval`, created and deleted 2026-08-28)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | How it was established                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The pool's `zcp` is a plain `type: zcp@1` import, not a git recipe**: `gitRepositoryUrl` and `buildFromGit` are `null`; its `ZEROPS_YAML` = `build.base ubuntu@22.04`, `deployFiles ./`, `run.base zcp@1`, port 8080 `httpSupport`, `initCommands` = [the `install.sh` fetch piped into `sudo sh`, `zcp init`, `sudo -E zcp init nginx`], `startCommands` = [`nginx: zcp service start nginx`, `vscode: zcp service start vscode`].                                                                                                                               | `printenv ZEROPS_YAML` on the container; `GET /service-stack/gt7tJZjDSk2zyH5XvNeAQQ` with the container's own token                                                                     |
| **A second `zcp@1` in the same project is created in ~3 min**: `zcli project service-import` `{hostname: zcpb, type: zcp@1, mode: NON_HA}` → `READY_TO_DEPLOY`; `zcli service push --setup zcp --no-git` with the exact `ZEROPS_YAML` above as `zerops.yml` → `ACTIVE` ~2–3 min after the push started (upload+trigger 55 s). `subdomainAccess` stays `false` after a zcli push (the auto-enable is zcp's deploy handler, CLAUDE.md trap) — `zcli service enable-subdomain` fixed it. The public URL is in the live env as `zcpb_zeropsSubdomain`.                  | the zcli calls above; `/etc/zerops-zembed/env.json`                                                                                                                                     |
| **Without `VSCODE_PASSWORD` a `zcp@1` is publicly open**: `zcpb` (plain import, no such env var) answered `302 → ?folder=/var/www` then `200` code-server HTML on its public URL — no `/zcp-login`, no cookie gate, exactly what the code-read row "if the env var is unset there is no auth at all" predicts. A "New project" path (S4) must set the password before enabling the subdomain, or use the first-class recipe that does.                                                                                                                              | `curl -sI https://zcpb-26a7-8080.prg1.zerops.app/`; deleted minutes later                                                                                                               |
| **The minimal copied credential set is `~/.claude/.credentials.json` alone** (mode 600). Before the copy: `claude -p` → `is_error:true`, "Not logged in · Please run /login". After: `result:"OK"` first try, and from `/var/www` `claude -p … --allowedTools mcp__zerops__zerops_discover` → `"z3-eval"` — the MCP works under the copied login through the per-container `~/.claude.json` `mcpServers` entry (there is no `/var/www/.mcp.json` on these containers). No merge of the stash `claude.json` was needed.                                              | `scp` of the stash file, then `claude -p "Reply with exactly OK" --output-format json` on `zcpb`; `claude --version` 2.1.250                                                            |
| **No refresh in the window; concurrent use did not conflict.** The stashed token's `expiresAt` = 2026-08-28 18:58:27 UTC (~8 h after its mint); unchanged on both containers after runs on each; the original `zcp` still answered `OK` after `zcpb`'s runs. The refresh race at/after `expiresAt` between two containers holding the same refresh token is UNTESTED — `questions.md` Q-12.                                                                                                                                                                         | `jq .claudeAiOauth.expiresAt` + mtime on both containers before/after                                                                                                                   |
| Codex in the container (`codex-cli 0.150.1`): `codex login --help` lists `--device-auth`, `--with-api-key`, `--with-access-token` (stdin). `~/.codex/` holds `config.toml`, `packages/`, `tmp/` — no credential file.                                                                                                                                                                                                                                                                                                                                               | `codex login --help` on `zcpb`                                                                                                                                                          |
| `claude setup-token` exists ("Set up a long-lived authentication token (requires Claude subscription)", no options) and this build wires the env var **`CLAUDE_CODE_OAUTH_TOKEN`** (string present in the installed binary).                                                                                                                                                                                                                                                                                                                                        | `claude setup-token --help`; `grep -a -o CLAUDE_CODE_OAUTH_TOKEN ~/.local/share/claude/versions/2.1.250`                                                                                |
| **T3 passes the parent environment to providers whole — no allow/deny list.** `mergeProviderInstanceEnvironment()` = `{...process.env, ...overrides}`; Claude adds only `CLAUDE_CONFIG_DIR` (deliberately not `HOME`, to keep OAuth/keychain lookup working); Codex the same spread. `CLAUDE_CODE_OAUTH_TOKEN`, `ANTHROPIC_API_KEY`, `ZCP_HARNESS`, any `ZCP_*` reach the provider process unmodified.                                                                                                                                                              | `../z3/apps/server/src/provider/ProviderInstanceEnvironment.ts:3-15`, `Drivers/ClaudeDriver.ts:129,139`, `Drivers/ClaudeHome.ts:17-34`, `Drivers/CodexDriver.ts:124,152` at `0e517b5b8` |
| **A service env write reaches the live env store in ~2–2.5 s, no restart**: `POST /service-stack/{id}/user-data` `{key, content, sensitive: true}` → `200` (async process); `/etc/zerops-zembed/env.json` carried the key ~2–2.5 s after the call returned and a fresh `ssh` session's `printenv` saw it immediately after. `sudo systemctl show-environment` never reflects it (84 s window) — an already-running process must re-read the file or be respawned. Delete is the UNNESTED **`DELETE /user-data/{id}`** (`/service-stack/{id}/user-data/{id}` → 404). | the curl calls from inside `zcpb` with its own token; a 0.5 s poll loop in one ssh session                                                                                              |

### S0.3 / S0.4 / S0.13 — git over the sshfs mount vs `ssh <host> git -C /var/www` (live on `z3-eval`: three throwaway `nodejs@22` dev services `s0git1..3`, `facebook/docusaurus` at `4cbfcfa1` depth-1 — 4 246 tracked files, `.git` 39 MB; `s0git1` additionally with a `pnpm` `node_modules` of 76 684 files, 82 716 files total; mounted on zcp with the product's exact `sshfs` primitive; all deleted after)

Versions: hosts git 2.34.1 (3 vCPU, 1280 MB); zcp git 2.43.0, sshfs 3.7.3 / FUSE 3.14.0. `/proc/loadavg` inside these containers reports the PHYSICAL NODE's load (14–28 with no busy process) — use `ps --sort=-pcpu` to judge idleness. Headline cells re-run at the end reproduced within 10–20 %. Raw scripts + logs: session scratch `s03/`.

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | How it was established                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **One T3 turn is 22 git invocations, not 12–20**: `captureCheckpoint` (`GitVcsDriver.ts:710-790`) = 7 (`rev-parse --git-common-dir`, `rev-parse --verify HEAD`, `read-tree HEAD`, `add -A -- .`, `write-tree`, `commit-tree`, `update-ref`), all but the first/last under a fresh `GIT_INDEX_FILE=<gitCommonDir>/t3-checkpoint-index-<uuid>` (it does not copy the real index — `read-tree HEAD` into a fresh one), run TWICE per turn (baseline + completion, `CheckpointReactor.ts:355, 509-526, 668-685`) each preceded by `rev-parse --verify --quiet <ref>^{commit}`; the status block (`GitVcsDriverCore.ts:1584`) = `status --porcelain=2 --branch` + `diff HEAD --numstat --` + `rev-parse … @{upstream}`; review = `ls-files --others --exclude-standard -z` + range diffs + one `diff --no-index` PER untracked file. Poller `DEFAULT_VCS_STATUS_REFRESH_INTERVAL` = 30 s per open cwd. | code read of the fork at `0e517b5b8`; the replicated sequence is `bench.sh`                                                            |
| **Full turn, clean tree (median / max ms)**: mount **12 699 / 13 170** · ssh no-mux 2 601 / 2 738 · **ssh + ControlMaster 1 370 / 1 916** · on the host 1 037 / 1 429. Checkpoint capture alone: mount 5 494 · no-mux 963 · mux 601 · host 452. `status --porcelain=2 --branch` back-to-back: mount 248 (cold 6 693) · no-mux 121 · mux 85 · host 62. One `rev-parse --show-toplevel`: 5 / 61 / 19 / 2. Re-run later: mount 13 534, mux 1 253, host 1 373. **The mount is 9× a multiplexed ssh and the ssh runner is 1.3× the host floor.**                                                                                                                                                                                                                                                                                                                                                       | 3 runs per cell, `date +%s%N` around each sequence, `GIT_INDEX_FILE` in the repo's `.git` (M1) / the host (M3, M4), 2026-08-28         |
| **The cost is the tree walk, not the change count**: 20 edited tracked files + 50 new untracked files written through the mount changed nothing — checkpoint mount 5 955 · mux 621 · host 445; status 262 / 74 / 65.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | same cells after the dirtying writes                                                                                                   |
| **Status at the real poll cadence is 1.4–2.2 s over the mount**, not 248 ms: the 30 s poller always outlives the 20 s `dcache_timeout`, so every production poll is the cold case (measured with 30 s gaps: 5 451, 1 419, 2 134, 2 159 ms).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `status --porcelain=2 --branch` on M1 with `sleep 30` between runs                                                                     |
| **The workspace rescan is a native `FileFinder` scan with a 15 s timeout** (`WorkspaceSearchIndex.ts`, `WORKSPACE_INDEX_SCAN_TIMEOUT_MS = 15_000`, `WORKSPACE_INDEX_MAX_ENTRIES = 25_000`; `WorkspaceEntries.ts` is only the per-keystroke single-level browse). A full recursive readdir of 94 629 dirents over the mount took 14 112, 6 056, 16 015, 2 619 ms across runs (the 20 s dir cache decides each) — twice past the timeout (→ `WorkspaceSearchIndexScanTimedOut`, index invalidated); on the host 409 / 489 ms; `ssh host find` 483 / 493 ms. Skipping `node_modules`: mount 375 (cold 1 379), host 41.                                                                                                                                                                                                                                                                               | `rescan.js` (node recursive `fs.readdir`) on zcp over the mount vs on the host; `find . -not -path './.git/*' -print` piped to a count |
| **ssh handshake: 59 ms unmultiplexed vs 8 ms multiplexed** (`ssh host true`, 20-iteration loops; a multiplexed `git rev-parse` round trip 19 ms). At 24 round trips per turn the master saves ~1.2 s per repo per turn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `-F /dev/null -o ControlPath=none -o ControlMaster=no` vs the container default                                                        |
| **zcp already ships ControlMaster for every host**: the managed `# ZCP:BEGIN … # ZCP:END` block in the container's `~/.ssh/config` sets `Host *` → `User zerops`, `StrictHostKeyChecking no`, `UserKnownHostsFile /dev/null`, `LogLevel ERROR`, `ControlMaster auto`, `ControlPath /tmp/ssh-mux-%r@%h:%p`, `ControlPersist 600`. A first "no-mux" run silently multiplexed until forced off with `-F /dev/null`. A driver should still pin the options itself, not inherit them.                                                                                                                                                                                                                                                                                                                                                                                                                  | `cat ~/.ssh/config` on the container; template `../zcp/internal/content/templates/ssh-config`                                          |
| **S0.4 forward — writes through the mount are write-through**: a new file, an edit of a tracked file, a delete, each made on zcp through the mount, were visible to host-side `git status` / `git diff --stat` at poll #0 of a 50 ms loop in all 3 trials (82–113 ms = the cost of `git status` over 82 k files, not a lag).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `vis_fwd.sh`                                                                                                                           |
| **S0.4 reverse — host-side changes reach the mount's file content and path lookups at once, but directory listings and stale entries lag exactly `dcache_timeout` = 20 s**: after a host-side create, `stat <newpath>` on zcp 11–23 ms but `ls <parent>` shows it only after 20 926–21 034 ms; a host-side edit / `git restore` shows new content in 14–33 ms; after a host-side delete, `stat` keeps succeeding for 20 836–20 949 ms. The product mount sets no cache options, so sshfs 3.7.3 defaults hold (`dir_cache=yes`, `dcache_timeout=20`, `dcache_max_size=10000`, `entry_timeout=1.0`, `attr_timeout=1.0`, `negative_timeout=0`, no `kernel_cache`, no `auto_cache`); `/proc/mounts` shows only `rw,nosuid,nodev,relatime,user_id=2023,group_id=2023`.                                                                                                                                 | `vis_rev.sh`, 100 ms polls, parent listing pre-warmed; `sshfs -h` for the defaults                                                     |
| **Fixing the reverse lag with mount options costs 3–27× on status**: a throwaway mount with `dir_cache=no,attr_timeout=0,entry_timeout=0` removed the lag (every reverse operation at poll #0) but `status --porcelain=2 --branch` over it = 6 620 ms median, cold 27 113 ms. Do not weaken the product mount; refresh host-side state over SSH instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `/home/zerops/s0mnt-nocache` (direct `sshfs`, `fusermount -u` after)                                                                   |
| **inotify over the mount never fires for host-side changes**: `fs.watch` on zcp (repo dir + a file) saw nothing in 10 s for a host-side append/create/delete; the same append made on zcp through the mount fired `change` at the moment of the write. Any watcher must poll over SSH.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `watch.js`                                                                                                                             |
| **S0.13 — three repos per turn (checkpoint + status each)**: mount sequential **21 019** ms · ssh+mux sequential 2 333 · **ssh+mux parallel 802** (per repo 600 / 1 105 / 752). Repos without `node_modules` cost the same as the one with (the `.gitignore` short-circuits the walk; the TRACKED count drives it). Fan-out across hosts is near-linear: three repos ≈ one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `multi.sh`, one ControlMaster per host, `&`/`wait` for the parallel cell                                                               |
| **`add -A` without a `.gitignore` is the 4-minute case.** zcp's host-side init (`ops.GitEnsureRepoHeadCommand`, `internal/ops/git_identity.go:78-81`) writes NO `.gitignore` and leaves an empty-tree HEAD. With docusaurus's `.gitignore` removed (76 684 untracked `node_modules` files): checkpoint on the host 9 846 ms, over ssh 9 444 ms, over the mount **245 536 ms**; `status` stays 63–100 ms on every method because it collapses untracked directories — it never reveals the problem, only the checkpoint pays.                                                                                                                                                                                                                                                                                                                                                                      | same bench with `.gitignore` removed on `s0git1`                                                                                       |

### S0.5 — base path `/z3/` under nginx on the 8080 origin (live on `z3probe`, nginx.conf hand-edited + `nginx -s reload`, 2026-08-28 13:35–13:42 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | How it was established                                                                                                   |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| **The whole z3 chain works through a path prefix on 8080, cookie-less**: `location /z3/ { proxy_pass http://127.0.0.1:3773/; proxy_http_version 1.1; Upgrade/Connection headers; proxy_read_timeout 86400s; }` placed beside `/healthz` outside the `__zcp_auth` gate → mint → `POST /z3/oauth/token` (form) `200` (same five scopes, `expires_in` 2 591 999) → `POST /z3/api/auth/websocket-ticket` `200` → `GET /z3/ws?wsTicket=…` **`101`** with a valid `Sec-WebSocket-Accept`; Node's `WebSocket` to `wss://…-8080…/z3/ws` opened in 43 ms and stayed open 60 s (native 3773 control: 27 ms, identical). `GET /` still `302 /zcp-login`; with the cookie, `/z3/` still reaches z3 (longest prefix beats `location /`). | `curl` chain from the laptop, no VPN; a Node script holding the socket                                                   |
| **Vite `base` is mandatory, not cosmetic**: `/z3/` serves the SPA index (19 884 B) whose refs are root-absolute (`<script src="/assets/index-….js">`, `<link href="/assets/index-….css">`): `/z3/assets/<x>` → `200`, `/assets/<x>` → `302 /zcp-login`. The page loads and every asset dies.                                                                                                                                                                                                                                                                                                                                                                                                                                | `curl -s …/z3/` + the two asset URLs                                                                                     |
| **A wrong base path fails silently, never as a 404**: with `proxy_pass http://127.0.0.1:3773` (no trailing slash, prefix kept) z3's SPA catch-all answers `200 text/html` for `/z3/.well-known/t3/environment` and `/z3/assets/*` alike — same on the native origin for any unknown prefix. A base-path bug looks like "the app loads but nothing works".                                                                                                                                                                                                                                                                                                                                                                   | one curl each, then restored                                                                                             |
| **No collision with code-server 4.134.0**: its top-level routes are `/`, `/_static`, `/healthz`, `/login`, `/logout`, `/update`, `/proxy/:port`, `/absproxy/:port`, `/robots.txt` — nothing under `/z3/`. Two side facts: nginx's exact `location = /healthz` already shadows code-server's own `/healthz`; and **`/proxy/3773/…` (behind the cookie gate) already reaches z3** (`/proxy/3773/.well-known/t3/environment` → z3's JSON) — a second door that exists today.                                                                                                                                                                                                                                                   | code-server's `src/node/routes/index.*` in the container; `curl -b __zcp_auth=… …/proxy/3773/.well-known/t3/environment` |
| **`z3probe`'s recipe never runs `install.sh`**: its `initCommands` are `cp /var/www/zcp-linux-amd64 /usr/local/bin/zcp`, `zcp init`, `sudo -E zcp init nginx` — a restart re-installs the DEPLOYED dev binary. "Restart = upgrade" (S0.2's `install.sh` half) is therefore measured on `z3-eval`, whose recipe is the platform's (S0.8 row), at the cost of one re-push (brief §4a).                                                                                                                                                                                                                                                                                                                                        | `printenv ZEROPS_YAML` on `z3probe`                                                                                      |

### S0.2 + S0.10 (restart half) — API restart of `z3probe` (2026-08-28 13:42:48 UTC, `PUT /service-stack/{id}/restart` with the owner's token → process `f9S43XpfS36CyshS2R9qgg` `stack.restart`)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | How it was established                                                                                                                     |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| **A restart is 19 s to z3 up, 17 s to `/healthz` answering, ~14 s of L7 `502`** ("Check if your application is running on a correct port"): call +0 s → last `200` −3 s → first `502` +3 s → kernel boot +8 s → binary re-copied +10 s → `zcp init nginx` / `initAt` +13 s → `zerops@z3` started +15 s → `/healthz 200` `z3Up:false` +17 s → `z3Up:true` +19 s; the `stack.restart` process `FINISHED` at +17.3 s. **The container is kept, not re-created** (`GET /service-stack/{id}/container` → same id `xjZMiUbpTIGA1VBIDwbrRQ`, `z3probe-runtime-4-376`, created 2026-08-27). | `curl -m 5 …/healthz` every 2 s from the laptop; `uptime -s`, binary mtime, `initAt`, `systemctl show zerops@z3` after; the process object |
| **A `zsc unit create`d unit survives a restart** — `zerops@s0probe.service` (`sleep infinity`) came back `active`, `UnitFileState=enabled`, `FragmentPath=/usr/lib/systemd/system/zerops@s0probe.service`. Former `questions.md` Q-01, now answered: anything registered at runtime through `zsc unit create` returns on its own.                                                                                                                                                                                                                                                   | created before the restart, inspected after                                                                                                |
| **State survives a restart**: `/home/zerops/.t3` (with `state.sqlite` — 3 threads / 6 messages / 3 turns / 1 project / 81 orchestration events, unchanged; the unconsumed pairing row still there), `/home/zerops`, `/home/zerops/app`, `/var/www` all kept their markers; **`/tmp` is tmpfs and is wiped**. `state.sqlite` grew 327 680 → 466 944 B with a new sha only because the 4.1 MB WAL was checkpointed on clean shutdown — a sha of the main file alone is not a state fingerprint while the server runs.                                                                 | markers written before; `node:sqlite` row counts + `sha256sum` before/after                                                                |
| **Live nginx edits are erased at boot**: `sudo -E zcp init nginx` re-rendered `nginx.conf` at +13 s and the hand-added `location /z3/` was gone (the `.s0bak` on the rootfs survived). Confirms `map.md`: container-side changes belong in the template.                                                                                                                                                                                                                                                                                                                            | `nginx -T` after the restart                                                                                                               |
| The pairing chain works again immediately after the restart on the native 3773 origin (mint → token, same five scopes → ticket → `101`).                                                                                                                                                                                                                                                                                                                                                                                                                                            | `curl` chain at +2 min                                                                                                                     |
| **Every boot of `t3 serve` mints an unconsumed `administrative-bootstrap` pairing link** with WIDER scopes than a normal mint (136- vs 93-char scope string), 5-min TTL, sitting in `auth_pairing_links` (created 0.5 s after the unit started). Whoever can read the z3 unit's stdout/journal gets an admin credential once per boot — S1 must disable or consume it in the Zerops environment.                                                                                                                                                                                    | `auth_pairing_links` rows via `node:sqlite` right after the restart; `journalctl -u zerops@z3`                                             |
| On `z3probe` the restart re-installed the SAME dev binary (sha unchanged, mtime moved) because its recipe copies `/var/www/zcp-linux-amd64` instead of running `install.sh` — the "upgrade" half of S0.2 is measured on `z3-eval` (see the S0.5 section's last row).                                                                                                                                                                                                                                                                                                                | `sha256sum /usr/local/bin/zcp` before/after                                                                                                |

### S0.10 (redeploy half) — `zcli service push` of `z3probe` with its own recipe (2026-08-28 13:46:21 → 13:48:50 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | How it was established                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A redeploy replaces the container and loses everything**: new container `vPjBZfeqTCyWg8wk8Sirgw` (`z3probe-runtime-5-376`, hostname `node-id-4` → `node-id-5`); all five markers gone (`/home/zerops/.t3`, `/home/zerops`, `/home/zerops/app`, `/var/www`, `/tmp`); `/home/zerops/.t3` recreated fresh (`state.sqlite` 4 096 B, 0 threads, 0 sessions, 0 events, all 43 migrations replayed, new `environmentId`); the `zsc`-created unit file gone; `/home/zerops` image-fresh. | `GET /service-stack/{id}/container` before/after; `node:sqlite` row counts; `ls` of every marker path                                                                                                             |
| **Zero downtime during the redeploy**: a 2 s `/healthz` poller (68 samples, `-m 5`) never saw anything but `200` — the L7 cut over from container 4 to 5 seamlessly, versus the restart's ~14 s `502` hole. End to end 2 min 29 s: push 13:46:21 → new container created 13:47:25 → boot 13:47:41 → `initAt` 13:47:49 → `zerops@z3` started 13:47:51 → `/healthz` shows the new `initAt` 13:48:02 → `z3Up:true` 13:48:50.                                                          | `zcli service push --projectId uHqwF26oTQ6V8D86HeABIA --serviceId Nq49woUlTBeFYdP0jg4DQA --setup zcp --no-git --working-dir <dir>` with the live `ZEROPS_YAML` + the binary its `deployFiles` demands; the poller |
| **Cold `npx --yes t3@0.0.35` on an image-fresh container = 58 s** from unit start to `z3Up` (13:47:51 → 13:48:49): H-05's residue, measured — the fresh-project first-boot cost and the argument for the local-bundle-first path (brief §4a).                                                                                                                                                                                                                                      | unit start time vs `/healthz` `z3Up` flip                                                                                                                                                                         |
| Restart keeps the container id, redeploy changes it — the cheap client-side signal for "the container changed under me".                                                                                                                                                                                                                                                                                                                                                           | the two `container` reads across S0.2 and this row                                                                                                                                                                |

### S0.9 (laptop half) — two clients on one container (`z3probe`, native 3773 origin, 2026-08-28 ~13:50 UTC; the phone half is owner-driven, `questions.md` Q-07)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | How it was established                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| **Two concurrent clients work at the transport level**: two mints → two token exchanges (identical scopes) → two tickets → two `WebSocket`s open at once (A +2.6 s, B +7.8 s), both `readyState 1` after 70 s; opening the second does not close the first; one RPC from each is served independently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Node 24 script from the laptop, two sockets             |
| **Wire protocol** (effect `RpcServer.toHttpEffectWebsocket` + `RpcSerialization.layerJson`, `apps/server/src/ws.ts:2488-2523`): a client frame is a JSON ARRAY of messages; `[{"_tag":"Ping"}]` → `{"_tag":"Pong"}` (~13 ms); a request is `{"_tag":"Request","id":"1","tag":"<method>","payload":{},"headers":[]}` → `{"_tag":"Exit","requestId":"1","exit":{"_tag":"Success","value":…}}`. The socket is silent until the client speaks (no hello frame). Method names: `packages/contracts/src/rpc.ts:209` `WS_METHODS`, `orchestration.ts:27` `ORCHESTRATION_WS_METHODS`. `server.getConfig` returns `auth: {policy: "remote-reachable", bootstrapMethods: ["one-time-token"], sessionMethods: ["browser-session-cookie","bearer-access-token","dpop-access-token"], sessionCookieName: "t3_session"}` and `cwd`.                                                                                                                                        | `server.probe` and `server.getConfig` over both sockets |
| **CORS is a wildcard on the z3 server, live**: `OPTIONS /oauth/token` with `Origin: http://localhost:5173` → `204`, `access-control-allow-origin: *`, methods `GET,POST,OPTIONS`, headers `authorization,b3,traceparent,content-type,dpop`; same `*` on a plain GET. Any origin can drive the pairing exchange today (D1's second exposure).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `curl -i -X OPTIONS` against the 3773 origin            |
| The POC mint's `origin` field is the **3773** URL (first declared port from `zeropsSubdomain`); under D2 it must become `<8080 origin>/z3` or the client pairs to a port that no longer exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `GET /z3-pair/<pw>` response                            |
| **Connecting the current `apps/web` client to a remote environment**: `npm run dev:web` in `../z3` serves the hosted client on port **5733** (`apps/web/vite.config.ts`, `PORT`); a mint with `--base-url <origin>` adds `pairUrl: "<origin>/pair#token=<credential>"`; entry paths (`packages/shared/src/remote.ts:188` `resolveRemotePairingTarget`, params `token`/`host`/`label`): (a) `http://<laptop>:5733/pair?host=<z3 origin>&token=<credential>` on the hosted client's own origin, (b) with zero environments `HostedStaticOnboardingState` (`apps/web/src/routes/_chat.index.tsx:139`) → `/settings/connections` → "Add environment" (`host` + `pairingCode`, `connection/onboarding.ts:35`). Credentials are one-time, 5-min TTL — mint per device right before connecting. `t3 serve` subcommands: `start, serve, pair, auth {pairing,session}, project {add,remove,rename}, service, triage` — no base-path flag, no offline thread creation. | code read at `0e517b5b8`; `pairUrl` verified live       |

### S0.2 (upgrade half) — API restart of `z3-eval`'s `zcp` (platform recipe with `install.sh`; 2026-08-28 14:07:03 UTC, `PUT /service-stack/gt7tJZjDSk2zyH5XvNeAQQ/restart`)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | How it was established                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A restart IS an upgrade**: the container held the hand-pushed dev binary (`v9.153.0-1-g64f63ae8`, sha `1f0255…`, 46 MB, mtime 12:26:47Z); after the restart `/usr/local/bin/zcp` was the latest RELEASE (`v9.153.0`, sha `57ffe70…`, 13.7 MB, mtime **14:07:12Z** = call +9 s) — `install.sh` re-ran from `initCommands` and replaced it; `zcp init nginx` ran at +13 s. Timeline from the public URL: `302` until the call, first `502` at **+2 s**, `302` again at **+16 s** (~14 s hole, same as z3probe); kernel boot at +6 s (`uptime -s` 14:07:09). | before/after `zcp version`, `sha256sum`, `ls --time-style=full-iso`; a 2 s `curl` poll of `https://zcp-26a7-8080.prg1.zerops.app/`; `journalctl -b` (`sudo … zcp init nginx` at 14:07:16) |
| **A project-scoped token can restart its own service**: the call was made from inside the container with its own `ZCP_API_KEY`; accepted (`200`, process `fimKPqkoQrm6LtpuIwfYbA`, `stack.restart`, `createdByUser` = the token user `zcp-z3-eval`). The product path (S4) uses the user's token, which S0.2 on z3probe showed works too.                                                                                                                                                                                                                   | the `PUT` response                                                                                                                                                                        |
| **What survives on the platform recipe**: `/home/zerops/.zcp/z3` (the dev z3 install, symlink + `node_modules`) and `~/.claude/.credentials.json` (the login: `claude -p` → `OK` right after boot); `/var/www` (AGENTS.md, CLAUDE.md, the dotdirs). Units `zerops@nginx/vscode/zerops` back; nothing else was registered. Re-push of the dev binary afterwards: `z3-dev-push.sh zcp` only (z3 needed no reinstall).                                                                                                                                         | `ls`, `claude -p "Reply with exactly OK" --output-format json`, `systemctl list-units`, the push log                                                                                      |
| Side fact: `sudo -E zsc unit remove <name>` deletes the unit file but leaves systemd with a stale `failed` record (`LoadState=loaded`, `UnitFileState=bad`, or `not-found failed`) until `systemctl daemon-reload` + `reset-failed` (or a reboot). Anything reading `systemctl list-units` for mount state (`ops/mount.go` does) can see a ghost after an unmount.                                                                                                                                                                                          | `systemctl show zerops@sshfs-s0git3` after the S0.3 cleanup, cleared with `daemon-reload` + `reset-failed`                                                                                |

### S6 PROVE — envelope on the wire: `structuredContent` vs text (live on `z3-eval`, 2026-08-28 14:39 UTC, temporary zcp patch returning the envelope as the handler's typed output; reverted)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | How it was established                                                                                                                                                                                        |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The Go MCP SDK (`go-sdk v1.6.1`, `mcp/server.go:355-390`) marshals a non-nil typed handler output into `structuredContent` and leaves the text block intact: raw JSON-RPC `tools/call zerops_workflow {action:"status"}` → `content: [1 text block, 3 649 B]` + `structuredContent` (233 B, keys `phase, environment, idleScenario, selfService, project, services, generated`).                                                                                                                                                                                                                                                                                                          | `printf` initialize/initialized/tools-call into `zcp serve` on the container, `python3` over the reply                                                                                                        |
| **Claude Code 2.1.250 REPLACES the model-facing tool result with `structuredContent` when it is present.** The `user` event's `tool_result.content` became the 216-byte JSON string of the envelope — the 3.6 KB markdown guidance never reached the model; the stream additionally carries `tool_use_result: {content: "<json>", structuredContent: {…}}`. So `structuredContent` cannot carry the envelope beside the LLM text: it would strip every atom the status renders. **Decision: the envelope rides inside the text** (a fenced JSON block after the markdown), which both Claude Code (`item.completed` raw block) and Codex (`mcpToolCall.result.content`) forward verbatim. | `claude -p '…zerops_workflow action="status"…' --output-format stream-json --verbose --allowedTools mcp__zerops__zerops_workflow` on the container; `python3` over the jsonl                                  |
| **Envelope-in-text verified live** (zcp `343bd2d2`, branch `feat/z3-envelope-wire`, pushed to `z3-eval` 14:57 UTC): the `zerops_workflow action="status"` tool result reaching Claude Code is one 3 892-byte text block that still starts with `## Status` (the atoms reach the model) and ends with a fenced `json zcp-envelope` block that parses back to the `StateEnvelope` (`phase`, `environment`, `idleScenario`, `selfService`, `project`, `services`, `generated`); `structuredContent` appears nowhere in the stream. Block size 140 B idle, ~1.7 KB with 4 services + a work session; the 32 KB MCP response cap has ~8 KB headroom above `ComposeBodyBudget` (24 KB).         | `claude -p 'Call zerops_workflow action="status" and then stop.' --output-format stream-json --verbose --allowedTools mcp__zerops__zerops_workflow` on the container, python regex over the last fenced block |

### S0.9 (web half) — two web clients on one z3 server (dev fork `t3@0.0.35-dev.0e517b5b8` started by hand on `z3-eval`'s zcp, loopback :3773 + SSH tunnel, cwd `/var/www`, real `claudeAgent` provider under the copied login; 2026-08-28 ~15:00 UTC; cleaned up)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | How it was established                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| **Two independent web clients share one thread, live.** Client A (own Vite origin, own pairing) created project `s09-var-www` (`workspaceRoot /var/www`) + a thread and got a real agent reply (`PONG`, turn done server-side in ~4.7 s). Client B (second Vite instance on another port ⇒ separate origin/localStorage, its own fresh credential) saw the project + thread with a live-activity dot right after pairing, with identical content. A new message dispatched into the thread by a third socket authenticated as A appeared in B's already-open view by **live push in ~502 ms** (DOM polled, no reload), the agent's reply followed. Three concurrent connections, zero eviction or "thread busy". Former Q-07 answered. | puppeteer-driven browser + browser-console WS calls (`orchestration.dispatchCommand` — the payload IS the command object, not `{command: …}`) |
| **Correction to the row above ("Connecting the current `apps/web` client…")**: `npm run dev:web` is the dev-runner harness — it wires the client to a co-located LOCAL backend and every route incl. `/pair?host=&token=` redirects into a local-pairing gate that ignores the query. The hosted-client behaviour is `cd apps/web && VITE_HOSTED_APP_CHANNEL=nightly PORT=<p> npm run dev` (no `VITE_HTTP_URL`/`VITE_WS_URL`) — then `isHostedStaticApp()` is true and `http://<origin>/pair?host=<z3 origin>&token=<credential>` pairs first try.                                                                                                                                                                                     | tried both                                                                                                                                    |
| **`auth.policy` is derived only from the server's `--host` bind string** (`apps/server/src/auth/EnvironmentAuthPolicy.ts` + `utils.ts` `isRemoteReachableHost`): `127.0.0.1`/`localhost`/`::1` ⇒ `loopback-browser` regardless of a proxy/tunnel in front. Behind nginx on loopback (the S2 layout) the server would mis-classify itself; the bearer → ticket → WS chain still worked under that label. S1 overrides the policy in Zerops mode.                                                                                                                                                                                                                                                                                        | `server.getConfig` over the tunnel + code read                                                                                                |
| **Bug**: the GUI "Add project" (folder-browse) dialog hangs on "Connecting…" indefinitely in this setup while raw `server.probe`/`server.getConfig`/`orchestration.dispatchCommand` over the same bearer answer instantly — one UI flow, not transport/auth. `questions.md` Q-13. Worked around by dispatching the create-project command directly; S2's `--auto-bootstrap-project-from-cwd` avoids the dialog on the product path anyway.                                                                                                                                                                                                                                                                                             | ~40 s wait in the browser; console RPCs                                                                                                       |

### Q-10 (first half) — Turnstile is enforced server-side (laptop, 2026-08-28 15:04 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                            | How it was established                                                                                                                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`POST /registration` without a Turnstile `token` is rejected with `400` code `cloudflareCaptchaVerificationFailed`** ("Cloudflare captcha verification failed. Please try again."), 39 ms, `access-control-allow-origin: *` — distinct from the field-validation `invalidUserInput`. The S0.7 row "captcha not a required field at the validation layer" holds only for the `{}` probe; a complete body IS captcha-checked. No account was created.           | full valid body with `claimZcpPool: true`, no `token`, from the laptop                                                                                                                                                                                               |
| **Both envelope carriers verified live** (zcp `d6e4db11`, merged into `feat/z3`): in one `claude -p` turn the prose `zerops_workflow status` result (3 892 B, markdown intact) yielded the fenced envelope and the JSON `zerops_mount action="status"` result (366 B) yielded its top-level `envelope` key — one extractor (JSON-first, then the last fence) picked both; `structuredContent` absent. Deploy result grows 119 → 1 287 B on a 4-service project. | `claude -p 'Call zerops_workflow action="status", then call zerops_mount action="status", then stop.' --output-format stream-json --verbose --allowedTools mcp__zerops__zerops_workflow,mcp__zerops__zerops_mount` on the container + `extract.py` (spec-z3 §1 rule) |

### Q-10 (answered) + S4 API facts (laptop, 2026-08-28 15:05–15:20 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | How it was established                                                                                                                     |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| **Zerops' Turnstile site key is hostname-bound: a page on any non-Zerops origin cannot obtain a token.** The production GUI uses `0x4AAAAAABkfI4SNvJav8428` (same as `environment.local.ts`; resolved from the live challenge-platform request on `app.zerops.io/registration`, the source files carry only a build placeholder). A minimal page with that key on `http://localhost:8765` fails at the widget with Cloudflare error **110200 "Domain not authorized"** before any token exists. In-z3 registration therefore needs a platform-side change (z3's hostnames added to the key's allowed domains) — Q-10 answered: no. | puppeteer on `app.zerops.io/registration` (`performance.getEntriesByType('resource')`) and on the local page; Cloudflare's error-code docs |
| **`GET /client/{clientId}/project` is lag-free; the search trails by ~0.5 s.** After `zcli project create` (LIGHT), the direct read returned the new project (`status: CREATING`) **498 ms** after the create was issued — ~600 ms before the create call itself returned — while `POST /project/search` needed 994 ms (one clean sample; a sequential-poll run read 1 853 vs 1 953 ms). A project row exists long before `ACTIVE`. The endpoint takes `statuses`, `limit`, `offset`, `nameContains` (`ListClientProjects`).                                                                                                       | two throwaway projects `s4probe`/`s4probe2` in org KRLS, created and deleted; 250 ms parallel polls                                        |
| **`POST /auth/refresh` returns the session fields at the TOP level** (`ResponseAuthFull`), unlike `/auth/login` (`ResponseAuthLogin`) and `/2fa/totp/login` which wrap them in `auth` — a client reading `body.auth` after a refresh silently signs the user out.                                                                                                                                                                                                                                                                                                                                                                  | spec + the S4.1 client tests                                                                                                               |
| Correction to the S0.11 row: `zcp studio topology` DOES emit **`mountPath`** (`/var/www/<hostname>`) for a service whose mount directory exists (`internal/tools/discover.go:127-132`, on the exported `EnrichWithMetaStatus` path) — the repo enumerator for S3/S6 rests on it. Managed types read like `valkey:single@7.2`, runtimes carry the OS prefix (`ubuntu/nodejs@22`); `adoptionState` values seen live: `zcp-self`, `adoptable`, `managed-dep`.                                                                                                                                                                         | live sample with a mounted `nodejs@22`, a valkey and an unmounted runtime, 2026-08-28 15:06 UTC                                            |

## Streams — live-verified on `z3-eval` (2026-08-28)

### S1 — the door (fork `z3-s1` at `ff5734de8`, merged into `z3` as `65487b468`; server started by hand on loopback with `T3CODE_ZEROPS_PROJECT_ID`/`T3CODE_ZEROPS_API_HOST`, reached through an SSH tunnel)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                           | How it was established                               |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| **`T3CODE_ZEROPS_PROJECT_ID` set ⇒ the descriptor flips**: `GET /api/auth/session` → `auth.policy: "remote-reachable"` (regardless of the loopback bind), `bootstrapMethods: ["zerops-identity","one-time-token"]`; without the variable the same binary answers `loopback-browser` / `["one-time-token"]` — every Zerops behaviour is gated by the env presence, not by where the request came from.                                          | check 1 and check 6 (second server without the vars) |
| **`POST /api/auth/zerops-identity {token}` is the door**: owner's token → `200` `{id, credential (12 chars, upstream's pairing shape), label: "Zerops OWNER", expiresAt (+2 min)}` in 0.10 s; the non-member's token → `403` `{_tag: EnvironmentOperationForbiddenError, code: operation_forbidden, reason: zerops_project_membership_required}` with no credential; `not-a-token` → `401` `{code: auth_invalid, reason: invalid_credential}`. | checks 2–4 from the laptop through the tunnel        |
| **The exchanged session is the membership window**: `POST /oauth/token` (token-exchange) → `expires_in: 899`, the five standard scopes, `token_type: Bearer` — not the 30-day one-time-token TTL; `POST /api/auth/websocket-ticket` → 192-char ticket; `ws://…/ws?wsTicket=…` + `server.getConfig` → `Exit/Success` with the same `auth` block. A member reaches a thread-capable socket with no pairing code (S1 acceptance line).            | check 5                                              |
| Runbook note: `pgrep -af bin.mjs` does not match the `.bin/t3` process — match on `t3 serve`; `nohup … &` inside one ssh call does not detach reliably — use `ssh -f` or `setsid … </dev/null`.                                                                                                                                                                                                                                                | cleanup                                              |

### S2 (zcp half, slices 1–2) — `zcp init` supervises z3 (zcp `03682093` on branch `feat/z3-init`, pushed to `z3-eval` 15:16 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | How it was established                                                                                                  |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **A plain `zcp init` brings z3 up**: step `Zerops Code (z3)` used the bundle already at `~/.zcp/z3` (no npm, no network), wrote `~/.zcp/z3.env` (0600: `T3CODE_ZEROPS_PROJECT_ID`, `T3CODE_ZEROPS_API_HOST`), registered `zerops@z3` via `sudo -E zsc unit create z3 "zcp service start z3"` (unit file `/usr/lib/systemd/system/zerops@z3.service`, enabled), wrote the marker `/var/www/.zcp/state/init-complete` = `{"initComplete":true,"initAt":…}`, and exited 0. 10 s later `http://127.0.0.1:3773/.well-known/t3/environment` answered and `/api/auth/session` showed `policy: remote-reachable`, `bootstrapMethods: ["zerops-identity","one-time-token"]` — the env file reached the supervised process. A second `zcp init` registered nothing (idempotent). | the push loop's `zcp init`, then a hand-run second `zcp init`; `systemctl status zerops@z3`; curls inside the container |
| The base-path probe is honest: with a bundle that predates `--base-path` the init log prints `! the installed z3 bundle does not advertise --base-path; z3 will answer under /z3/ but its assets will not resolve` and the supervisor omits the flag.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | init output on the container                                                                                            |

### S2 (zcp half, slice 3) + S1.4 — the public `/z3/` door through nginx (zcp `6b998342` on `z3-eval`, z3 `z3-s1` at `8051697c8` under `zerops@z3`; 15:21–15:25 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | How it was established                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| **`GET /healthz` on the public 8080 origin is static and gate-free**: `200 application/json`, `cache-control: no-store`, body `{"initComplete":true,"initAt":"2026-08-28T15:21:25Z"}` (the marker `zcp init` wrote); code-server's own `/healthz` is shadowed; `/` still `302 /zcp-login`.                                                                                                                                                                                                                                                                                                                                                                                                                   | `curl -i https://zcp-26a7-8080.prg1.zerops.app/healthz` |
| **`location /z3/` proxies z3 outside the cookie gate**: `/z3/.well-known/t3/environment` → `200 application/json` (`serverVersion 0.0.35`, no `basePath` yet — the installed bundle predates the flag); `/z3/api/auth/session` → `policy: remote-reachable`, `bootstrapMethods: ["zerops-identity","one-time-token"]`. During the unit's restart the location answers `502` (the L7 error page) for ~8 s.                                                                                                                                                                                                                                                                                                    | curls from the laptop, no cookie                        |
| **code-server's port-proxy door is closed for the z3 port only**: `/proxy/3773/` → `404`, `/absproxy/3773/` → `404`, `/proxy/8081/x` → `302` (login) as before.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | curls from the laptop                                   |
| **S1.4's CORS allowlist holds through nginx**: `OPTIONS /z3/oauth/token` with `Origin: https://evil.example` → no `access-control-allow-origin` (the S0.9 wildcard is gone); `http://localhost:5733` → echoed; `http://127.0.0.1:5733` → none (trusted by hostname, like the welcome bridge).                                                                                                                                                                                                                                                                                                                                                                                                                | curls from the laptop                                   |
| Until the S2 server/web slice is pushed the SPA at `/z3/` still references `/assets/…` (root-absolute) — the S0.5 finding, unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `curl -s …/z3/ \| grep src=`                            |
| **S1.4 through the public door** (fork `8051697c8` under `zerops@z3`, via nginx `/z3/`): a VALID ticket with `Origin: https://evil.example` on `/z3/ws` → `403` `{code: operation_forbidden, reason: origin_not_allowed}` — refused before the ticket is read; the container's own origin → `400` (upgrade attempted, curl is not a websocket); no `Origin` → `400` likewise; Node's `WebSocket` (no Origin) to `wss://…/z3/ws?wsTicket=…` opens and `server.getConfig` answers. CORS preflight on `/z3/oauth/token`: evil → no allow-origin header, `http://localhost:5733` → echoed, `http://127.0.0.1:5733` → none. The non-member's token at the public door → `403 zerops_project_membership_required`. | s1-verify pack from the laptop, 15:24 UTC               |

### S2 (z3 half) — base path end to end (fork `z3-s2` at `b72194310`, merged into `z3`; verified on a local build, live check pending the next push)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | How it was established                                                                                                                                                      |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **nginx strips, the server only knows its public prefix**: `t3 serve --base-path /z3` (or `T3CODE_BASE_PATH`) makes the descriptor advertise `"basePath":"/z3"` and every absolute URL the server emits join it (`pairUrl` → `<origin>/z3/pair#token=…`); routes stay at `/`. The web bundle bakes the prefix at build time: `VITE_BASE_PATH=/z3 vp run --filter @t3tools/web build` (the push loop forwards `Z3_BASE_PATH`); `index.html` uses `%BASE_URL%`, the manifest went relative, TanStack router got `basepath`, the window-origin target is origin+prefix. Default build is byte-shape identical to upstream.                       | local build served on `127.0.0.1:3799`: descriptor JSON with `basePath`, all six shell assets `200` under `/z3/`, `/thread/42` → the shell, the full auth chain + `/ws` 101 |
| **12 client sites dropped a path prefix, not the 10 the plan mapped** — the two unmapped ones were the worst: `packages/client-runtime/src/rpc/http.ts` `remoteApiBaseUrl` (the real request base of every API call) and the DPoP `htu` builder (a proof bound to a URL the request never reaches); plus `state/assets.ts` (every image/attachment would 404), `settings/pairingUrls.ts`, mobile `connection/pairing.ts` and `cloud/linkEnvironment.ts`. Six upstream tests that pinned "a base URL's path is noise" now assert the prefix survives.                                                                                          | S2z-1/S2z-2 commits + tests                                                                                                                                                 |
| **A tolerant ingress strip is structurally impossible**: any middleware passed to effect's `HttpRouter.serve` leaves its `R` unsolvable (499 type errors, three typings tried). Instead a request still carrying the configured prefix gets a **404 JSON naming the mistake** (proxy must strip; `proxy_pass` needs its trailing slash); unmatched `/api`, `/oauth`, `/.well-known`, `/ws` are 404 JSON in production too (previously dev-only, so they returned 200 HTML). S0.5's silent mode is dead; consequence: code-server's `/proxy/3773/` and direct-to-3773 browsing do not work with a `/z3/`-built bundle — use the `/z3/` origin. | `b72194310`; local `/z3/.well-known/t3/environment` on the unstripped path → 404 with that text                                                                             |
| The client-side prefix check is the descriptor's advertised `basePath` (catches "both ends healthy, prefixes disagree"); a server stating no `basePath` (every older one) is still accepted.                                                                                                                                                                                                                                                                                                                                                                                                                                                  | client-runtime tests                                                                                                                                                        |
| Pre-existing on `z3`, unrelated: `apps/server/src/entrypoint.test.ts` "matches through a symlinked entrypoint" fails on this laptop (checked by stashing).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | S1 and S2 both reproduced it on the untouched branch                                                                                                                        |

### S4 (web) — platform facts found while building the client (laptop, read-only, 2026-08-28 afternoon)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | How it was established                                                                                                                                                                    |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The GUI's zcp import YAML** (`ZeropsYamlBuilder.generate()`): `hostname` `zcp` (a second one is **`zcp1`**, `getNextServiceName` counts the bare name as 0 then `max+1`), `type zcp@1`, `maxContainers 1`, `enableSubdomainAccess true`, `verticalAutoscaling.minRam 2`, `envSecrets {VSCODE_PASSWORD, ZCP_VSCODE_AUTH_ENABLED, ZCP_VSCODE}`, `zeropsYaml{…}` whose first `initCommand` is `zcpInstallCommand()` — the `curl https://zerops.io/zcp/install.sh` fetch piped into `sudo sh` (with ` -s <version>` only when pinned), NOT `zcp install`. No `mode:` field. The 16-char `VSCODE_PASSWORD` is generated client-side (`libs/zef/src/core/utils.ts:451-467`, `byte % 62`, skewed). | `../frontend-legacy/apps/zerops/src/modules/core/zerops-services/yaml-templates/yaml-builder.service.ts:44-299`, `shared-scripts.template.ts:28`, `zcp-pool-claim-base.effect.ts:661-668` |
| **The API publishes its import-YAML JSON schema**: `GET /settings/import-project-yml-json-schema.json` (82 KB). The fork's generated document validates against it (ajv), and the schema is not vacuous (missing `hostname`, non-integer `port`, unknown top-level key are rejected with specific messages).                                                                                                                                                                                                                                                                                                                                                                                  | fetched and validated on the laptop                                                                                                                                                       |
| `POST /client/{clientId}/project` body the GUI sends: `{name, description:"", tagList:[], location:<fastest>, clientId, mode:"LIGHT", maxCreditLimit:null, userRoles:[]}`; `location` is nullable and no public endpoint lists locations (`OutDtoLocation` exists only nested in a project) — sending `null` lets the platform pick.                                                                                                                                                                                                                                                                                                                                                          | GUI trace + two probe projects created with `location` absent                                                                                                                             |
| A pre-z3 container answers `/healthz` with `302 → /zcp-login` (the cookie gate; no location yet) — the "predates z3" signal under `redirect: "manual"` is an opaque redirect. `/auth/login` with an unknown e-mail answers `400 userNotFound` (distinct from a wrong password — user enumeration on a public `CORS *` endpoint, platform-side).                                                                                                                                                                                                                                                                                                                                               | curls against `localflow/zcp` and `z3-eval/zcp` before the S2 push; one bogus login                                                                                                       |

### Integration build on `z3-eval` — fork `z3` at `2f7ed13f0` (S1 complete, S2 base path, S3.1–3.2, S4.1–4.7, S6 feeds) under `zerops@z3`, zcp `6b998342`; pushed with `Z3_BASE_PATH=/z3` at 15:41 UTC

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | How it was established                                                             |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| **The supervisor's capability probe flips on its own**: with a bundle that advertises `--base-path`, `zcp service start z3` logs `args=[… serve --mode web --host 127.0.0.1 --port 3773 --base-path /z3 --base-dir /home/zerops/.t3 …]` and the boot prints `Zerops Code is ready. / Sign in with your Zerops account - members of this project are let in by …` — no `Token:`, no `/pair`, no QR (S1.6 live).                                                                                                                                                                                                                                                                                                                                                            | `journalctl -u zerops@z3` after the push                                           |
| **Base path live through nginx**: `GET /z3/.well-known/t3/environment` → `200 application/json` with `"basePath":"/z3"`; the shell at `/z3/` references `/z3/assets/index-*.js`, `/z3/assets/index-*.css`, `/z3/apple-touch-icon.png` and each asset answers `200` with its content type; `GET /z3/api/nope` → `404 application/json` `{"error":"not_found","path":"/api/nope","detail":"No server route matches this path. Server routes are never answered with the client shell."}` (the loud failure replacing S0.5's silent 200). `/z3/api/auth/session` → `bootstrapMethods: ["zerops-identity","one-time-token"]`, `sessionMethods: ["bearer-access-token","dpop-access-token"]` (no cookie, S1.5 live).                                                           | curls from the laptop, no cookie, no VPN                                           |
| Push-loop note: `Z3_BASE_PATH=/z3 ./eval/scripts/z3-dev-push.sh z3` is the whole delivery; the first attempt of this push printed nothing and left the old build in place — always read the script's exit code and `npm ls t3` on the container, never a grep of its output.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | two consecutive runs                                                               |
| **The two-probe readiness contract classifies live containers correctly** (S4.5, header-less GETs, `redirect:"manual"`, parse before trusting): `z3-eval` (`/healthz` JSON + `/z3/.well-known/…` JSON with `basePath`) → **ready**; `localflow/zcp` (pre-z3: `/healthz` → 302 login) → **predates-z3**; `z3probe` (POC: `/healthz` JSON with the old `z3Up:true` but no `/z3/` mount) → **initializing** until its cap — the POC's `z3Up` field is meaningless under the new contract and the probe ignores it. A mid-boot container answers the L7's 502 (nginx starts only after every `initCommands` entry finished), rendered as "restarting", never as predates-z3. Turnstile on the z3 origin reproduces `110200` and renders the "Sign up at app.zerops.io" state. | S4.5 probe run from the laptop against the three containers, 2026-08-28 ~15:45 UTC |

### S1.7 + S1.6 store + S6 feeds — live pack 3 on the integration build (`t3@0.0.35-dev.13f15dacf`, public `/z3/`, 2026-08-28 ~15:50 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | How it was established                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------ |
| **`exec.run` works through the door and is scoped**: `{command:"zcp",args:["version"]}` → `{exitCode:0, stdout:"zcp v9.153.0-12-g6b998342 …", timedOut:false}` in 241 ms; `sh -c "exit 7"` → `exitCode:7` as a SUCCESS exit; `sleep 30` with `timeoutMs:500` → `{exitCode:null, timedOut:true}` in 514 ms; an injection-shaped argument arrives verbatim in stdout (no shell). A session from a plain pairing credential (scopes `orchestration:read orchestration:operate terminal:operate review:write relay:read`) gets `EnvironmentAuthorizationError` with `requiredScope:"exec:operate"`; the identity door's session carries `… relay:read exec:operate`.                                                                                                       | one WS from the laptop, member bearer vs a `t3 auth pairing create` credential      |
| S1.6 live: after the 15:41 boot the unit journal has no new `Token:` line (the 4 present are all from earlier boots); `GET /api/auth/pairing-links` with the member bearer → `403 insufficient_scope` (`access:read`) — a member session cannot even list the store.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `journalctl -u zerops@z3 -b`, curl                                                  |
| **Blocker found: the `zerops@z3` unit's process tree carries only `HOME`, `PATH` and the `T3CODE_*` file** — none of the container's `ZCP_*`/`ZEROPS_*` service variables a login shell gets. So `zerops.topology.get` → `{available:true, degraded:true, reason:"auth: No authentication found: set ZCP_API_KEY or log in with zcli", services:[]}` while the same `zcp studio topology` over ssh lists the three services; and a turn under z3 never produced an envelope in 90 s (its `zcp serve` MCP starves the same way). Fix: `zcp service start z3` loads the live env store (`/etc/zerops-zembed/env.json`, 199 keys, root-owned 0644) under the `z3.env` overrides — in progress.                                                                            | `/proc/<MainPID>/environ` of the unit; the WS calls; `zcp studio topology` over ssh |
| The topology contract on the wire is a flat `services[]` with a `group: runtimes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | data                                                                                | infrastructure` field per entry (not nested groups). | `zerops.topology.get` response shape |
| **The workspace index cannot be moved off the mount by swapping in `find`/`grep`**: `WorkspaceSearchIndex`'s `search`/`searchContents` are `finder.fileSearch`/`directorySearch`/`mixedSearch`/`grep` inside `@ff-labs/fff-node`'s closed Rust `FileFinder` (frecency ordering, smart case, per-file caps, grep cursors, time budget); `@t3tools/shared/searchRanking` is client-side only; `FileFinder.create` exposes no ignore configuration. Whether the scan trips over a mount with `node_modules` (S0.3: 2.6–16 s full, 375 ms skipping `node_modules`) is decided by one live probe (`FileFinder.create({basePath:"/var/www"})` + `waitForIndexReady(15000)`); if it trips, raise `WORKSPACE_INDEX_SCAN_TIMEOUT_MS` for Zerops before reimplementing anything. | S3 code read at `65487b468`; probe pending                                          |

### S4.6 + the unit-environment fix — live on `z3-eval` (fork `beef10c47` under `zerops@z3`, zcp `683110d0`; 2026-08-28 ~16:00–16:10 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | How it was established                                                                                |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **The S4 acceptance line holds from the hosted client**: signed in with the owner's Zerops account (token seeded through the storage adapter), the picker listed 22 containers across both orgs (11 ready with origins, 11 unavailable each naming its reason), **Connect** on `z3-eval` → descriptor → `POST /z3/api/auth/zerops-identity` → `/z3/oauth/token` → environment registered → Settings › Connections shows `node-id-1.runtime.zcp.zerops` with a live green dot. **No pairing code anywhere.** A non-member 403 maps to `ConnectionBlockedError{reason:"permission"}` only when the body is contract-shaped; a loose body reads "transient" and would retry forever. | S4.6 live run in the hosted client (puppeteer), 2026-08-28                                            |
| **`/healthz` carries no `Access-Control-Allow-Origin`**, so a browser on another origin gets `TypeError: Failed to fetch` from a header-less `fetch` — a simple GET still needs the header on the RESPONSE to be readable; the z3 descriptor under `/z3/` sends `*`. Client mitigation shipped: the descriptor is the authority, `/healthz` only separates "z3 still coming up" from "predates z3". nginx fix (`add_header Access-Control-Allow-Origin "*" always;`) in progress.                                                                                                                                                                                                 | `fetch('…/healthz', {redirect:'manual'})` from the page vs `fetch('…/z3/.well-known/t3/environment')` |
| **`zcp service start z3` now gives the child the container's live env**: after the fix the t3 process carries 111 `ZCP_*`/`ZEROPS_*` variables incl. `ZCP_API_KEY`, `VSCODE_PASSWORD`, and the shell's `PATH` (with `~/.local/bin`), under the `T3CODE_*` file; before it carried only `HOME`/`PATH`/`T3CODE_*`.                                                                                                                                                                                                                                                                                                                                                                  | `/proc/<child pid>/environ` before/after the push                                                     |
| **`--auto-bootstrap-project-from-cwd /var/www` created no project at any of three boots** (only the verifier's soft-deleted `s6-lifecycle-probe` exists); no "startup auto-bootstrap welcome failed" warning in the journal. Under investigation: flag/positional parsing (see the next row).                                                                                                                                                                                                                                                                                                                                                                                     | `projection_projects` via `node:sqlite`; `journalctl -u zerops@z3`                                    |
| **`/healthz` now readable cross-origin**: zcp `21421bb4` adds `Access-Control-Allow-Origin: *` (`always`) to both `/healthz` branches — public origin answers `200 application/json`, `access-control-allow-origin: *`, `cache-control: no-store`, `{"initComplete":true,"initAt":"2026-08-28T16:11:45Z"}`; the cookie-gated code-server location stays CORS-less (`TestRunNginx_HealthzHasCORSForCrossOriginProbe`).                                                                                                                                                                                                                                                             | `curl -i https://zcp-26a7-8080.prg1.zerops.app/healthz` after the push, 16:12 UTC                     |

### S6 feeds live + S4 picker probe (fork `beef10c47` / `z3-s4` `988116a4f`, 2026-08-28 ~16:10–16:20 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | How it was established                                                               |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| **Topology feed live**: `zerops.topology.get` → `{available:true, degraded:false, project:{id,name}, services:[s3git1 (group runtimes, mounted:true, mountPath), s3git2 (same), zcp (group infrastructure, mounted:false, adoptionState zcp-self)]}`. A fresh `get` showed an imported valkey ~1 s after zcli's create completed (zcli itself blocked ~16 s) and its absence ~9 s after the delete. **But `subscribeZeropsTopology` pushed only its initial Chunk** — no frame on import or delete within 90 s / 40 s — under investigation.                                                                                                                                                                                     | pack 4 from the laptop over the public `/z3/ws`                                      |
| **Lifecycle feed live**: after `thread.turn.start` ("call zerops_workflow status…"), `zerops.lifecycle.get` returned the envelope (`phase idle`, `idleScenario adopt`, 2 services `bootstrapped:false`) and `recentTools:[{toolName:"zerops_workflow", status:"completed"}]` **9.5 s** after the turn started. Pack 3's empty result was the unit-env bug (the MCP starved), not the reducer.                                                                                                                                                                                                                                                                                                                                    | pack 4                                                                               |
| **An idle WebSocket through the public origin closes at exactly 60 s** (code 1006) with no traffic after the subscribe; nginx's `proxy_read_timeout` is 86400 s, so this is the Zerops L7's idle timeout. Any subscription needs outbound traffic more often than that — the product client's keepalive interval is being checked.                                                                                                                                                                                                                                                                                                                                                                                               | two watcher connections in pack 4 (the second survived with a 20 s poll)             |
| **From a browser, a pre-z3 container is indistinguishable from an unreachable one**: it sends no CORS header on any route, so every `fetch` throws (`TypeError: Failed to fetch`) where curl sees a 302. The picker therefore offers "Enable Zerops Code" for both cases (a restart helps either way); the ambiguity disappears once every container has restarted through the release gate. Live: the owner's account → 1 connected (`z3-eval`), 10 ready (all pre-z3, each offering Enable), 11 unavailable with reasons.                                                                                                                                                                                                      | S4 per-row probe run in the hosted client                                            |
| A 5xx on either probe now short-circuits to `unreachable` (descriptor 502 + `/healthz` redirect was being read as "predates z3" and offered a restart to a booting container — caught by the RED test).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `z3-s4` `988116a4f` tests                                                            |
| **`t3 serve --auto-bootstrap-project-from-cwd <dir>` was inert upstream**: the `serve` handler passes `forceAutoBootstrapProjectFromCwd: false` (`apps/server/src/cli/server.ts:31-34`) and the config resolver (`cli/config.ts:347-355`) is `firstSomeOf(force, headless-rule, flag, env)` — two `Some(false)` entries sit above the flag, so the startup guard was never entered and nothing was logged. Fixed on the fork (flag leads; command default and headless rule become defaults; env stays last): local A/B 0 → 1 project titled after the cwd + 1 thread within ~2 s of "ready", same ids on restart. `apps/server/src/cli/config.test.ts` "honors an explicit auto-bootstrap flag over the serve command opt-out". | `z3-boot` `9fe6b4c15`, laptop run from source; container check pending the next push |
| **`subscribeZeropsTopology` does not push on a live socket** (fork `beef10c47`): one WS, subscribe → Chunk in 17 ms; `{"_tag":"Ping"}`/`Pong` every 5 s for 189 s (37 round trips, socket OPEN throughout — so pack 4's 60 s close was the idle L7 timeout and the product's 5 s ping covers it); a valkey imported (zcli returned after 47 s) and later deleted (21 s) produced **zero further Chunks** in two 60 s windows, while a one-off `zerops.topology.get` between them listed the new service. The feed's `get` is fresh; nothing publishes to subscribers live, though the fake-zcp tests pass — under investigation on the container.                                                                                | pack 5 from the laptop, raw frames in the session scratch `s5verify/frames.jsonl`    |

### S3 — live audit on `z3-eval` (fork `beef10c47`→`4d0230b4a` under `zerops@z3`, rig `s3git1`/`s3git2` docusaurus + throwaway `s3git3` node repo with 19 308 untracked `node_modules` paths and no `.gitignore`; 2026-08-28 16:18–17:1x UTC; 6 940 argv samples at 20 Hz)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | How it was established                                                                                    |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| **The S3 invariant holds**: zero bare `git` processes on the container during a two-service turn; zero argv carrying a `/var/www/<host>` path; every git call is `ssh -l zerops -o ControlMaster=auto -o ControlPath=/tmp/ssh-mux-%r@%h:%p -o ControlPersist=600 -o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR -o ServerAliveInterval=15 -o ServerAliveCountMax=3 <host> env GIT_INDEX_FILE=/var/www/.git/t3-checkpoint-index-<uuid> GIT_AUTHOR_NAME=… git -C /var/www …` (only `GIT_*` cross the wire — no `PATH`, no `ZCP_API_KEY`, no `GIT_TRACE2*`). Two-service turn 24.9 s wall incl. agent thinking; one ControlMaster per host, created once and reused across the session.                                                           | `ps -eo args` sampler at 20 Hz during a WS-driven turn; `ssh -O check`                                    |
| **sshd `MaxSessions` (default 10) is not a limit**: 16 concurrent sessions over one master → 16/16 ok in 3 s (sequential would be 48 s); the cap of 4 per host has room.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 16-way parallel probe on `s3git1`                                                                         |
| **Policy live**: a `thread.create` carrying a `worktreePath` persisted `worktreePath: null`; capabilities `pullRequests:false`, `vcsStackedActions:false` (`repositoryIdentity:true`); 0 `git fetch` in 6 940 samples.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | read model + capabilities + sampler                                                                       |
| **Multi-repo checkpoints live**: one turn editing both services wrote the identical ref name `refs/t3/checkpoints/<thread>/turn/1` on BOTH hosts with different oids per repo; the turn diff `files[]` = `s3git1/README.md`, `s3git2/README.md` (prefixed, sorted); restore fanned out with each repo's own source (`restore --source <oid>` + `reset --quiet`), zero `git clean`, and an untracked file planted on the host after the checkpoint survived the revert.                                                                                                                                                                                                                                                                                                                         | `for-each-ref refs/t3` on both hosts; the turn diff; `?? uploads-audit.txt` after restore                 |
| **Restore visibility through the mount is immediate**: host and mount showed the reverted tracked files in the same 10 Hz poll, 6.27 s after dispatch (the fan-out itself, not mount lag). S0.4's 20 s applies to directory listings and stale entries, not to rewritten tracked files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 10 Hz poll on both sides                                                                                  |
| **The native FileFinder skips `node_modules` on its own — S3.5 is unnecessary**: with 32 764 dirents under `/var/www` (incl. a 19 308-file `node_modules` in a repo with no `.gitignore`), `waitForIndexReady(15000)` → `ok:true` in 2 027 / 3 646 / 1 979 ms; results are mount-relative (`s3git1/…`) with a `gitStatus` per file; `s3git3` is indexed but no `node_modules` path is ever returned (`lodash`, present only there, finds nothing).                                                                                                                                                                                                                                                                                                                                             | `FileFinder.create({basePath:"/var/www"})` probes on the container, 3 runs                                |
| **The `add -A` guard FAILED live**: the probe `ls-files --others --exclude-standard -z` ran over SSH (1 165 676 bytes, 19 308 paths) but `s3git3` still received turn/0 and turn/1 and the turn/0 tree holds 19 310 files — `node_modules` went into the checkpoint. Locally the truncation, the guard and the refusal all work against a real repo with 6 000 untracked files (`ZeropsUntrackedProbe.test.ts`), so the defect is specific to the SSH path (overflow not reported, or the probe failing into a swallowed `false`). Second defect by reading: `probeUntrackedOverflow` caches the VISIT, not the verdict — a repo would be refused at most once per server lifetime. Both being fixed. `git status --porcelain` shows one line `?? node_modules/` for all of it (S0.3's point). | the audit turn on `s3git3`; `ls-tree -r` of the checkpoint; `z3-s3` `c3239b441`                           |
| **Nothing prunes checkpoint refs on thread delete** (the brief's checkpoint rule 3): the audit's thread left `refs/t3/checkpoints/<thread>/turn/*` on both hosts — fan-out pruning being added.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `for-each-ref` after cleanup                                                                              |
| **Guard root cause was memoisation, not SSH**: `probeUntrackedOverflow` cached the visit (`probed.has(cwd)`), and the reactor drives the baseline TWICE per turn (`turn-start-requested` and `message-sent`, `CheckpointReactor.ts:854`) through one fan-out — first call refused, second call skipped the probe and captured the tree it had just refused; the completion capture followed. Fixed by probing every time (one `ls-files` capped at 256 KB, ~0.1 s over the mux); caching the verdict was rejected because a repo would stay refused after its `.gitignore` arrives. A whole-stack test with a `/bin/sh`-executing fake `ssh` (`ZeropsGuardOverSsh.test.ts`) shows the spawner path was correct all along. Live re-run pending the next push.                                   | `z3-s3` `031803591`; RED "keeps refusing while the repository still overflows, however often it is asked" |

### S6 web — live on `z3-eval` (fork `4d0230b4a` under `zerops@z3`, hosted client on the laptop paired with a one-time credential; 2026-08-28 ~17:20 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | How it was established                                                                                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The service map renders live data**: the right panel offers a "Zerops" surface only because the topology feed answered `available:true`; it showed Runtimes `s3git1`/`s3git2` (`nodejs@22` — the `ubuntu/` prefix stripped — `ACTIVE`, mount paths from zcp's `mountPath`), Data `s6live3` (`valkey:single@7.2`), Infrastructure `zcp` (`zcp@1`, the only row with "Open" because only it carries a `subdomainUrl`), zcp's adoptable warning verbatim, then quick actions. `doorbellConnected:true` renders nothing.                                                                                                                                                                                                                                                                                                                                           | puppeteer on `http://localhost:5190` paired to `https://zcp-26a7-8080.prg1.zerops.app/z3`                                                           |
| **Quick-action gating holds on real data**: "Deploy" and "Show logs" render, "Add Redis" is absent because a valkey exists; clicking Deploy composes exactly `Deploy s3git1.` (the mounted runtime) without sending.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | same session                                                                                                                                        |
| The strip and the cards appear only once a thread has run a `zerops_*` tool — S3's git-audit threads carry none, so a read-only look shows neither (by design).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | same session; a tool-running turn follows                                                                                                           |
| **Lifecycle strip live**: after one read-only turn (`zerops_workflow action="status"`) the thread header shows `infrastructure ready · 2 services` (tone idle) — the envelope round-tripped zcp → lifecycle feed → strip; the strip counts the ENVELOPE's `services[]` (workflow-tracked runtimes) while the map lists the TOPOLOGY's (incl. the valkey and zcp). A layout bug visible only on a real screen (the strip ran underneath the absolutely-positioned panel controls) was fixed. Cards did not render on this pass because the container's build predates the server-side projection seam — the degrade path ("Used 2 tools") worked as designed.                                                                                                                                                                                                     | the same paired session, `z3-s6ui` `0c3ef1033`                                                                                                      |
| **Resolution of the "subscription never pushes" rows above — the probe, not the feed.** effect-rpc streams are flow-controlled per request: the server writes a Chunk, closes a latch and waits for the client's `Ack` (`RpcServer.ts:401-421`, opened at `:191-193`; frame `[{"_tag":"Ack","requestId":"<id>"}]`, `RpcMessage.ts:96-122`). A raw WS probe that never acks receives exactly ONE Chunk and no Exit on a healthy socket; `RpcClient` acks automatically, so every real client works. Live from inside the container with the Ack line added: subscribe → Chunk; `zcli` import of a valkey → a Chunk containing it **0.5 s** after the import started, then settling Chunks (doorbell push as designed, `doorbellConnected:true`). Runbook: every raw probe of `subscribe*` methods must ack each Chunk.                                            | `z3-s6c` `c8e7c87d3` (two tests: publish from an RPC handler fiber; a late second subscriber); the acked probe on the container, 17:29 UTC          |
| **Card decoders hold against byte-exact live payloads** (`zerops_verify` 901 B → `{kind:"verify", hostname:"s3git1", status:"healthy", checks:[service_running pass, error_logs pass]}`; a failed `zerops_deploy` 2 048 B → the error card with `SSH_DEPLOY_FAILED`, suggestion, `failureClass:"network"`), captured by asking the agent to paste the result text verbatim (the client only ever sees the 84-char summary). Two facts the Go structs did not show: a live error message is MULTI-LINE (zcli's log embedded — five lines), and a verify document carries `workSessionState` AND a top-level `envelope` beside the `VerifyResult` fields — the card ignores both on purpose (the envelope reaches the client only through the lifecycle feed, so a card and the strip can never disagree).                                                         | `z3-s6ui` `0ddc3f878`, `apps/web/src/zerops/cards/liveFixtures.ts`                                                                                  |
| **Lifecycle persistence in the wild**: the z3 unit restarted mid-turn (a push) — "Provider session did not survive a server restart" — and the strip kept reading `infrastructure ready · 2 services` straight through it (the persisted latest envelope, S6 server D6).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | the same thread across the 17:32 UTC restart                                                                                                        |
| **Former Q-14 — the adopt gate on `zerops_deploy` is by design skipped until the FIRST adoption in a project**: `requireAdoption` (`internal/tools/guard.go:83-134`) returns nil while `<stateDir>/services/` does not exist ("gate activates once the first service is adopted, a clean migration path"); on `z3-eval` no bootstrap ever ran, so `zerops_deploy targetService="s3git1"` reached zcli and failed with `SSH_DEPLOY_FAILED` (`File zerops.yml not found`; its `failureClassification.category:"network"` is the transport default — a separate mislabel). The S6 envelope slice (`d6e4db11`) only appends `Envelope` to success responses after the deploy — not a regression. The mismatch is `zerops_discover`'s unconditional `ADOPT_REQUIRED` promise (`internal/tools/discover.go:258`); tightening the gate or the wording is an owner call. | code read + `claude -p` reproduction on the container, zcp `21421bb4`                                                                               |
| **The `add -A` guard holds live after the fix** (fork `2415aa536`): `s3git3` recreated with no `.gitignore` and 18 380 untracked paths (1.1 MB of `ls-files -z`); one turn appending to all three READMEs settled in 19.6 s; `s3git1`/`s3git2` got turn/0 + turn/1, **`s3git3` got 0 refs**, the turn diff lists only the two, and the thread carries the activity `No checkpoint for /var/www/s3git3: This repository has thousands of untracked files and no usable .gitignore…`. Argv (5 552 samples): the probe ran per capture (8 samples), zero `add -A`/`write-tree`/`commit-tree`/`update-ref` against `s3git3`, zero argv on a mount path. The 11 bare `git` processes seen were another agent's Codex CLI running `git ls-remote https://github.com/openai/plugins.git` — not a repository operation.                                                  | S3 re-run on `z3-eval`, 17:4x UTC; `s3git3` unmounted and deleted after                                                                             |
| **Checkpoint refs survive `thread.delete`** (confirmed live): immediately after deleting the audit threads, both hosts still carried 5 of their `refs/t3/checkpoints/<thread>/turn/*` — pruning on delete being added (S3).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `for-each-ref refs/t3` on both hosts after the RPC deletes                                                                                          |
| **A one-time-token pairing dies after 15 minutes in a Zerops project**: S1's membership window (`expires_in` 899) caps every session issued while `T3CODE_ZEROPS_PROJECT_ID` is set — including sessions from `t3 auth pairing create` (which have no Zerops token to re-mint with) and DPoP's 1 h default. Observed: a hosted client paired at 17:19 UTC showed "The environment credential is invalid" at ~17:34, Reconnect did not clear it, a reload lost the strip too (the client cache) — misread at first as the push invalidating sessions. Fix in progress: scope the window to `zerops-identity` sessions only.                                                                                                                                                                                                                                       | S6 UI live pass; `auth_sessions` TTL per `EnvironmentAuth.ts`                                                                                       |
| **The auto-bootstrap works live once nothing else owns `/var/www`**: after S3 deleted its residue project and the unit restarted (build `cfbed0289`), the container had a `www` project at `/var/www` with a first thread, and a freshly paired hosted client landed in it. But the thread defaults to **Codex** (`getAutoBootstrapDefaultModelSelection` is hardcoded `codex` + `DEFAULT_MODEL` upstream, `serverRuntimeStartup.ts:172`), which is unauthenticated here, and the model picker shows "No models found" — a product defect for the landing; fix: default to the first authenticated provider (Claude on these containers).                                                                                                                                                                                                                        | S6 UI pairing at 17:41 UTC; `projection_projects` via `node:sqlite`                                                                                 |
| **Cards live** (fork `cfbed0289`): the verify card renders `s3git1 · healthy` with two green checks; the deploy-error card renders `SSH_DEPLOY_FAILED` with its seven zcli log lines as separate lines (the multi-line fix in the wild), the diagnostic hint and a `network` badge; the strip sits clear of the panel controls; the prose `zerops_workflow status` result gets no card (by design — the envelope reaches the client only via the lifecycle feed). Correction to an earlier reading: the model picker is not empty — it shows "No models found" until the user types (typing "claude" lists nine models). UX note for the owner: cards render inside the collapsed "Used N tools" group, so they are hidden until expanded — whether Zerops cards escape the fold is a product call.                                                              | S6 UI live pass in the paired hosted client                                                                                                         |
| **Bootstrap default still `codex` live after the first fix** (fork `ffd6f8697`, `www` deleted before the restart): the re-bootstrapped `www` and its first thread carry `{"instanceId":"codex","model":"gpt-5.6-sol"}` — the provider snapshot is cold when the startup phase runs (Claude's `ready`/`authenticated` arrives seconds later), so the "first authenticated provider" rule finds none and falls back to upstream. A bounded wait for resolved provider statuses is the follow-up.                                                                                                                                                                                                                                                                                                                                                                   | `projection_projects`/`projection_threads` via `node:sqlite` 14 s after the restart                                                                 |
| **Bootstrap default fixed live** (fork `1e4d1f1a9`): with `www` purged and the unit restarted, the re-bootstrapped `www` and its first thread both carry `{"instanceId":"claudeAgent","model":"claude-sonnet-5"}`. Root cause of the earlier miss: every managed provider publishes a synchronous "probe pending" snapshot (`enabled && !installed && status==="warning"`, the registry's own `isPendingInitialProbe`) and the real probe runs on a forked fibre — the bootstrap now waits (≤ 30 s, 250 ms polls of `getProviders`) until NO provider is still probing, then picks Claude → Codex → any ready; an empty registry is not decidable; a settled registry costs one read. Outside a Zerops environment the upstream Codex default is byte-identical.                                                                                                 | `projection_projects`/`projection_threads` via `node:sqlite` 40 s after the restart; `ZeropsBootstrapModel.test.ts`, `serverRuntimeStartup.test.ts` |
| **Ref pruning verified live** (fork `ffd6f8697`): a thread in `www` checkpointed on both hosts (2 refs each); `thread.delete` via RPC removed exactly those four (18 → 14 refs across both hosts) and left every other thread's refs untouched — including the four a pre-pruning build had left behind at 17:57 (an accidental control). READMEs restored, `git status` clean on both hosts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `for-each-ref refs/t3` on `s3git1`/`s3git2` before/after                                                                                            |
| **The membership window follows the door** (fork `ffd6f8697`): a `t3 auth pairing create` credential exchanges to `expires_in 2591999` (upstream's 30 days), the identity door's to `899`; the scoping is stored as `BootstrapGrant.method` on the pairing link (persistence now uses the contract's bootstrap-method schema — it had its own copy that would have rejected `zerops-identity` at write time). Timed 20-minute survival probes running.                                                                                                                                                                                                                                                                                                                                                                                                           | pack 6, T0 = 18:02:19 UTC                                                                                                                           |

## Freeze — the hard fork (laptop, 2026-08-28 ~20:00 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | How it was established                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| **Frozen upstream base = `f94a0d646`** (`upstream/main` on 2026-08-28), tag `upstream-base-2026-08-28`; the product branch `z3` became `main` (`c537c53f4` at the rename); the fork's previous `main` (8 POC-era commits: brand, POC Zerops page, fork README, iOS build brief) is preserved as tag `main-poc-2026-08-28` and is contained in `zerops-poc`. `origin/z3` remains as an alias until deleted by hand.                                                                                                                                                          | `git tag`, `git branch -f main z3`, `git merge-base --is-ancestor`; pushed                   |
| **Provider work is not self-contained**: of 85 upstream commits touching `apps/server/src/provider`, `packages/effect-codex-app-server`, `packages/effect-acp` in the 60 days before the freeze, **37 (44 %) also change non-test code outside those paths** — `apps/web/src/components` 56 files, `apps/mobile/src/features` 34, `apps/server/src/orchestration` 33, `packages/client-runtime/src/state` 21, `contracts/settings.ts` 10, `contracts/orchestration.ts` 8 (vendored `.repos/`, tests, docs, lockfile excluded). Basis of the import/port split in `fork.md`. | `git log upstream/main --since='60 days ago' -- <paths>` + `git show --name-only` per commit |
| Upstream velocity around the freeze: 117 / 156 / 197 / 127 non-merge commits in the four weeks before 2026-08-28; 51 of the last 28 days' commits touched the provider layer. `v0.0.35` (2026-08-26) is the last stable tag; `v0.0.36` exists only as nightlies.                                                                                                                                                                                                                                                                                                            | `git log upstream/main --since/--until`, `git tag --sort=-creatordate`                       |
| Our surface at the freeze: 120 new files, 100 modified upstream files (about half tests), **0 modified and 0 new under the provider paths above**.                                                                                                                                                                                                                                                                                                                                                                                                                          | `git diff --name-status upstream-base-2026-08-28...main`                                     |

### Freeze progress (laptop, 2026-08-29 morning)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | How it was established                                                                                                 |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **The import zone is exactly two packages and both are standalone**: `packages/effect-codex-app-server` and `packages/effect-acp` depend only on `effect`/`@effect/*` and import nothing from `@t3tools/*` or `apps/`. Pinned by `imported.lock` (`upstream` = commit `f94a0d646`, one tree OID per path); `node scripts/imported-lock.ts --check` fails with one line per drifted path.                                                                                                                                                                                                                                                                                                                                                  | dependency read + grep over both packages; `--write` against `upstream-base-2026-08-28`; a hand-corrupted OID → exit 1 |
| **The zone architecture test** (`scripts/z3-zone-architecture.test.ts`) asserts the ported zone has no `zerops` imports and that `apps/server/src/zerops/**` reaches no provider internals, against an exact known-violations list (`ZeropsLifecycle.ts`, `ZeropsTopology.ts`, `ZeropsPolicy.test.ts` at the freeze; the SPI bus slice empties it). Live-mutation RED on both rules.                                                                                                                                                                                                                                                                                                                                                      | `vp test run scripts/z3-zone-architecture.test.ts`                                                                     |
| **CI = `ci.yml` only**; 13 upstream workflows deleted (release/publish/preview/EAS/relay-deploy/labels/vouch/size/transfer-report) with their helper files; Blacksmith runner labels replaced by GitHub-hosted `ubuntu-latest`/`macos-latest` (owner to confirm); the check job runs the lock check + zone test first.                                                                                                                                                                                                                                                                                                                                                                                                                    | workflow read-through; YAML parse; every check-job command run locally                                                 |
| **Tailscale removed end-to-end**: 55 files across `packages/tailscale`, server config/lifecycle, `t3 pair/serve` flags, desktop endpoint provider + settings + IPC, web Settings row, contracts (`RemoteOpenTargetKind` keeps `mdns`), the `vp run dev --share` tailnet workflow, docs. `git grep -i tailscale` hits only this ledger. Kept: `browserTargetResolver.ts` `.ts.net` private-host classification (a security check on pasted URLs, not the feature).                                                                                                                                                                                                                                                                         | per-package typecheck + tests green after each of 6 commits                                                            |
| **Version line 0.1.0** (`apps/server`, `apps/web`, `apps/desktop`, `packages/contracts`); mobile keeps its own App Store version (`1.0.4`). `t3 --version` and the descriptor's `serverVersion` both read `apps/server/package.json`; the push loop emits `0.1.0-dev.<sha>`. The desktop updater resolves its GitHub target from `T3CODE_DESKTOP_UPDATE_REPOSITORY` / `GITHUB_REPOSITORY` at build time — no hardcoded upstream owner.                                                                                                                                                                                                                                                                                                    | grep + `cli.ts pack` proof (`t3-0.1.0-dev.check.tgz`)                                                                  |
| **Model manifest** refreshed at runtime from `krls2020/z3` `main` instead of upstream's; the on-disk cache key is a fixed path, so the old cache is simply superseded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `ModelManifest.ts` constant; 7/7 tests                                                                                 |
| **Four SDK-faithful Claude recordings** exist (`apps/server/src/spi/fixtures/claude/`): recorded through `@anthropic-ai/claude-agent-sdk` `query()` 0.3.250 (CLI 2.1.251, model `claude-opus-5[1m]`) with the adapter's options and BOTH callbacks (`canUseTool`, `onUserDialog`) teed as control lines. `AskUserQuestion` reaches `canUseTool` (answer shape `{behavior:"allow", updatedInput:{questions, answers:{"<question>":"Blue"}}}`); auto-mode approves benign MCP calls without a callback; an SDK `interrupt()` right after the first `tool_use` yields `result:error_during_execution` with `terminal_reason:"aborted_streaming"`; the adapter's own `interruptTurn` uses `close()` (no further messages), not `interrupt()`. | recorder `apps/server/src/spi/recording/record-claude.mjs` on `z3-eval`, 4 runs 5.9–12.2 s                             |

### S7 — the OAuth flag's `sensitive` visibility (laptop + `z3-eval`, 2026-08-29 morning)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | How it was established                                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A `sensitive=true` service user-data entry is invisible to the Zerops GUI's agent-flag read.** The GUI reads flags from its `UserDataEntity` store = `POST /user-data/search`, where a sensitive entry comes back with `content:"REDACTED"` even for the org OWNER token; `serviceStack.userData` (stack GET) and `activeAppVersion.userDataList` carry ONLY SYSTEM entries (9 / 18 on `z3-eval`'s zcp) — no USER entry, sensitive or not. Verbatim content of a sensitive entry appears only on `GET /service-stack/{id}/env`, `GET /service-stack/{id}/user-data`, `GET /user-data/{id}`, which the GUI's flag path does not use. Consequence: a flag written by `zcp agent mark-oauth` (sensitive=true at the time) renders as NOT authorized in the GUI → S7-0 makes the writer non-sensitive. | probe with the container's integration token and the OWNER token; org-wide scan of 1 371 user-data entries (747 SYSTEM, 621 USER non-sensitive, 3 USER sensitive) |
| All 19 `ZCP_AGENT_OAUTH_*` entries in org KRLS are `sensitive:false` (GUI-written: `POST /api/service-stack/{id}/user-data {key, content:'true'}` with no `sensitive` field). `z3-eval`'s zcp carries `ZCP_AGENT_OAUTH_CLAUDE_CODE=true` + `ZCP_AGENT_AUTH_TYPE_CLAUDE_CODE=oauth`, both non-sensitive, both present in `/etc/zerops-zembed/env.json`; `zcp agent mark-oauth claude-code` there → `{"ok":true,"agent":"claude-code","key":"ZCP_AGENT_OAUTH_CLAUDE_CODE","changed":false}`.                                                                                                                                                                                                                                                                                                           | same probe                                                                                                                                                        |
| **The FE import path now produces sensitive entries the GUI then cannot read back**: service `zcp` in project `zerops-code` (created 2026-08-28 15:53 via the FE with `envSecrets`) has `VSCODE_PASSWORD`, `ZCP_VSCODE`, `ZCP_VSCODE_AUTH_ENABLED` as `sensitive:true` → `hasVscode` reads `REDACTED` (inferred from code + API data; GUI rendering not observed). `z3-eval` (pool-shaped, 10:57) and `localflow` (July) have all of them non-sensitive. Platform/GUI item for the owner.                                                                                                                                                                                                                                                                                                            | org scan + `zerops-services.utils.ts:207-258` read-through                                                                                                        |
| Gotchas: the container's `ZEROPS_ServiceId` is NOT the API service id (404 `serviceStackNotFound`) — `ZEROPS_StackId` is; `POST /user-data/search` needs a `clientId` filter or answers 400; an ES bool filter `{"name":"sensitive","operator":"eq"}` → 400 (filter client-side). The container's integration token shows `NO_ACCESS` on the client in `/user/info` but reads the whole project.                                                                                                                                                                                                                                                                                                                                                                                                     | same probe                                                                                                                                                        |

### S7-1 — watcher + scheduler findings (laptop, 2026-08-29, this repo's pinned Effect)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | How it was established                                   |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| **Interrupting an Effect-wrapped `fs.watch` stream (`FileSystem.watch` + `Effect.forkScoped`) via scope closure crashes the scheduler**: `TypeError: self.addObserver is not a function` inside `MixedScheduler`, whether or not a watch event ever fired. A second shape hits the same crash with no `fs.watch` at all: two forked fibers racing (one draining a `PubSub`, one debounce-driven doing a `FileSystem` op). Consequence: the S7 credential watcher is plain Node `fs.watch` (`ZeropsAgentAuthWatcher.ts`, `watchWithFallback`), bridged into Effect through a `Queue` (`Queue.offerUnsafe` → one forked, debounced drainer) — that bridge does not hit the bug. | minimal repros in isolation; `ZeropsAgentAuthIo.test.ts` |
| A `Schedule.exponential` retry nested inside a fiber already woken from `Stream.debounce`'s clock wait silently gives up after one attempt (only when nested that way; the retry is correct standalone). The "`ZeropsCliFailed` retries then succeeds" case is therefore not unit-tested — S7-3 live is the proof.                                                                                                                                                                                                                                                                                                                                                            | repro + the removed flaky test's comment                 |
| `it.layer(NodeServices.layer)` merges a virtual `TestClock` by default in this repo's `@effect/vitest` setup — anything using real timers needs `{ excludeTestServices: true }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `ZeropsAgentAuthIo.test.ts`                              |
| Watching a lone FILE with `fs.watch` misses "change" events on this machine (Node's documented caveat); watch the parent directory filtered by basename instead — the same tolerant attach welcome.js uses (fall back to the parent until the target exists, then re-attach).                                                                                                                                                                                                                                                                                                                                                                                                 | live on the laptop; `ZeropsAgentAuthWatcher.test.ts`     |
| Streaming zerops RPCs are named flat camelCase after their key (`subscribeZeropsTopology`, `subscribeZeropsLifecycle`, now `subscribeZeropsAgentAuth`); the dotted form is only for non-streaming get/refresh verbs. The `WsRpcGroup` scope map in `auth/RpcAuthorization.ts` is compile-time exhaustive — a new route needs its scope entry.                                                                                                                                                                                                                                                                                                                                 | `packages/contracts/src/rpc.ts`, `RpcAuthorization.ts`   |

### SPI live sanity on `z3-eval` (`t3@0.1.0-dev.900e03d9a`, laptop, 2026-08-29 09:33–09:45 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | How it was established                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The SPI refactor (owned bus + `toolCall` enrichment) keeps the lifecycle path intact end to end**: a real Claude turn (`claude-sonnet-5`, prompt `Call zerops_workflow action="status", then call zerops_mount action="status", then reply done.`, 14.9 s) produced lifecycle chunks `zerops_workflow inProgress` → `+zerops_mount inProgress` → `zerops_workflow completed + envelope` (87 ms after the server's completion stamp, 88 ms BEFORE the thread projection's own completed item) → `zerops_mount completed`, the latest envelope winning; `recentTools` updated in place per `itemId` (2 entries, never 4); the topology feed re-read on completion (`readAt` advanced; `zcp studio topology` spawns at the 3 s cadence for the 90 s nudge window) and re-emitted nothing because nothing changed (`contentSignature` excludes `readAt`, by design); journal: zero enrichment / unrecognized / WARN lines for the two Claude MCP items. | door → WS (`subscribeZeropsTopology`, `subscribeZeropsLifecycle`, `orchestration.dispatchCommand thread.create/turn.start/thread.delete`), 2 Hz `ps` sampler on the container; artifacts in the session scratch |
| Day-1 door recipe unchanged: `POST /z3/api/auth/zerops-identity` (134 ms) → `POST /z3/oauth/token` token-exchange (53 ms, `expires_in` 899, scopes incl. `exec:operate`) → `POST /z3/api/auth/websocket-ticket` → `wss://…/z3/ws?wsTicket=…`; Ping every 5 s, Ack after every Chunk.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | same run                                                                                                                                                                                                        |
| Ordering worth knowing for the web: the lifecycle feed publishes ~90 ms before the thread projection's completed item — a strip driven by the lifecycle feed updates before the card for the same tool.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | same run                                                                                                                                                                                                        |
| Rig drift since day 1: `weatherdash` (alpine/nginx, adopted, mounted) exists → envelope `idleScenario: bootstrapped`, 3 services; residue z3 projects (three `www` rows, `s6-lifecycle-probe(-2)`, `s3-audit`, 12 threads); upstream's `thread.delete` is a soft delete (`deletedAt`), the snapshot still lists the row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `GET /api/orchestration/snapshot`, `zerops.topology.get`                                                                                                                                                        |

### S7-3 — agent auth live on `z3-eval` (zcp `6be2f654`, `t3@0.1.0-dev.b2d5d044f`, 2026-08-29 09:46–10:11 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | How it was established                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| **The agent-auth feed reacts to the credential file within half a second**: `mv ~/.claude/.credentials.json` aside → `credPresent:false`, `state:reconnect` in 357 / 351 ms; back → `credPresent:true`, `state:authorized` in 467 / 378 ms; the picker's provider feed follows with a fresh Claude check 1.7 s (probe cache hit) / 5.0 s (real probe ≈3.5 s) after the restore. `zcp agent mark-oauth` was not spawned during the session — the per-process `markedOAuth` latch was set at unit boot (credential present) and never resets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `subscribeZeropsAgentAuth` + `subscribeServerConfig` over the door WS; 100 ms / 20 ms `ps` samplers on the container      |
| **F1 — upstream's Claude probe is a false positive for logout**: with the credential file absent, `claude auth status` → `{loggedIn:false, authMethod:none}` and `claude -p` → "Not logged in", yet `ClaudeProvider.ts` (~:892-896) derives `auth.status:authenticated "Claude Max Subscription"` from the SDK init message's `subscriptionType`/`tokenSource`, which Claude still reports from `~/.claude.json`'s account. Consequence: `providerAuth` never flips to `unauthenticated` for Claude, the "verify before mark" gate is inert for a planted/stale file, the picker never shows Claude logged out. Fix: the owned feed verifies with `claude auth status` (JSON `loggedIn`) / `codex login status` itself, not the provider probe.                                                                                                                                                                                                                                                               | forced `server.refreshProviders` with the file aside; pid-level timing of the probe                                       |
| **F3** — the "targeted refresh" serves `ClaudeDriver`'s capabilities cache (`CAPABILITIES_PROBE_TTL` 5 min): inside the TTL a refresh returns in ~220 ms with no `claude` spawn; a real probe (`claude --version` + a stream-json init spawn, ~3.5–4 s) only when expired. **F4** — each real probe writes `~/.claude/backups` and `~/.claude/sessions`, which the dir-level watcher sees as a second event (one echo re-check ~4 s later, from cache; not a loop). **F5** — the success path (watcher → refresh → spawn) logs nothing; `journalctl -u zerops@z3` stayed empty for the whole session.                                                                                                                                                                                                                                                                                                                                                                                                         | same run                                                                                                                  |
| **F6 — the container-served client was not a hosted-static build**: `z3-dev-push.sh` built the web with `VITE_BASE_PATH` only, so `isHostedStaticApp()` was false and `/z3/` redirected to `/pair`; a seeded session and a door-minted credential pasted into the pair form both fail (`browser_session_unsupported`, HTTP 403 at `auth/http.ts:234`); `/z3/zerops` signs in but lists `z3-eval` as "Connected" with no button (`useConnectedZeropsOrigins` counts the primary origin regardless of auth). The push script now sets `VITE_HOSTED_APP_CHANNEL=container`. Workaround used for the run: `VITE_HOSTED_APP_CHANNEL=latest vp dev` on the laptop → landing → picker → Connect (door from `localhost`).                                                                                                                                                                                                                                                                                             | bundle grep (`VITE_HOSTED_APP_CHANNEL:""`), `_chat.tsx:190` redirect                                                      |
| **Card + terminal move work**: in a draft thread the card lists `claude-code` (reconnect) and `codex` (not-authorized) with Sign-in buttons; "Sign in to Codex" opens the terminal and runs `codex login --device-auth` — output: `https://auth.openai.com/codex/device` (rendered in link colour) + a one-time code; synthetic Ctrl+C on `textarea.t3-ghostty-input` cancels. **F8 — `claude /login` first shows an interactive menu** (1. Claude account with subscription / 2. Console / 3. 3rd-party; Esc to cancel); only after Enter does it print "Browser didn't open? Use the url below to sign in (c to copy)" and a ~330-char `https://claude.com/cai/oauth/authorize?…` URL **soft-wrapped over 4 rows as dim plain text — NOT link-coloured by Ghostty** (no OSC 8 seen); synthetic Esc did not cancel, SIGINT did. A copy-URL affordance is needed for Claude on the web too, not only on the phone. **F7** — with the terminal pane open the card overlaps the empty-state heading (cosmetic). | puppeteer on the dev client + pid sampling on the container; screenshots in the puppeteer store `s7-08`, `s7-11`, `s7-12` |
| D5 confirmed on the new binary: `ZCP_AGENT_OAUTH_CLAUDE_CODE` is `sensitive:false`, `lastUpdate` unchanged today (`mark-oauth` → `changed:false`); the stack GET's `userData` / `activeAppVersion.userDataList` hold only SYSTEM vars.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `GET /service-stack/{id}/env`, `POST /user-data/search`                                                                   |
| **F6 resolved**: the container-served client boots as a hosted-static app only with `VITE_HOSTED_APP_CHANNEL` ∈ {`latest`, `nightly`} (`hostedPairing.ts` `configuredHostedAppChannel` rejects any other value — `container` silently fell through to `/pair`); `z3-dev-push.sh` now builds the web with `VITE_HOSTED_APP_CHANNEL=latest` (`Z3_HOSTED_APP_CHANNEL` overrides) and `https://zcp-26a7-8080.prg1.zerops.app/z3/` renders the Zerops sign-in landing again (Email / Password / Sign in · Create one · Connect a backend manually) on `t3@0.1.0-dev.7c035c759`. A leftover browser session for the origin does not change the gate (verified with cleared storage).                                                                                                                                                                                                                                                                                                                                | `curl` of the served bundle (`HOSTED_APP_CHANNEL:'latest'`), puppeteer on the public URL, 2026-08-29 10:20 UTC            |

### S5-4 — the activity relay re-shelled for Zerops (laptop, 2026-08-29)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | How it was established                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| **`infra/relay` is now a plain Node service over direct Postgres**: `src/server.ts` on `@effect/platform-node` serves the same `RelayApi` groups (`mobile`, `link`, `token`, `server`, health/metadata); `db.ts` = `PgClient.layerConfig(DATABASE_URL)` + Drizzle; `scripts/migrate.ts` applies `migrations/postgres/**` in order (statement-breakpoint split, `relay_migrations` table). Deleted: the Cloudflare Worker/Alchemy shell, Hyperdrive, Queues, Tunnel/DNS, Axiom, `ManagedEndpoint*`, `ManagedTunnelLimits`, `EnvironmentConnector`, `RelayDpopClientGroup`, most of `RelayClientGroup` (now `RelayLinkGroup` = challenge + link). 152 tests against fakes (no live Postgres in the suite — the repo convention).                                                                | `git log main -- infra/relay`; `vp test run` in `infra/relay`                                |
| **Durable APNs queue** replaces the Cloudflare queue behind the one-method seam `ApnsDeliveryQueueSender.send`: table `relay_apns_delivery_jobs` (`job_id` PK dedupe via `onConflictDoNothing`, `available_at`, `lease_until`, `attempts`, `state` queued/leased/done/dead), `leaseNext` = `SELECT … FOR UPDATE SKIP LOCKED` + `UPDATE` in one transaction, backoff `30s·2^(n-1)` capped 10 min, dead-letter at attempt 5, expired-lease recovery + 24 h expiry + DPoP-nonce prune on a 5-min in-process loop. Lease exclusivity is proven at the state-transition level with fakes, not under two racing connections.                                                                                                                                                                        | `ApnsDeliveryJobStore.ts`, `ApnsDeliveryWorker.ts` + tests (`TestClock` for recovery/expiry) |
| **Relay auth = Zerops identity**: `verifyBearerToken` does `GET /user/info` with the caller's token (`ZeropsAuth.ts`); principal = Zerops user id; `/v1/client/dpop-token` takes the Zerops token as `subject_token`; Clerk and every `CLERK_*` key removed. **Project binding**: the environment link proof now REQUIRES `zeropsProjectId` + `endpointOrigin`; the relay verifies membership (`GET /project/{id}`) and that the origin belongs to a subdomain-enabled service of that project (`GET /project/{id}/service-stack`, URL rebuilt the way zcp's `BuildSubdomainURL` does; a bare `zeropsSubdomainHost` prefix fails closed).                                                                                                                                                     | `ZeropsProjectBinding.ts` + tests with a fake Zerops API                                     |
| **Server side**: `makeCloudLinkProof` refuses outside Zerops mode and stamps `zeropsProjectId` (`T3CODE_ZEROPS_PROJECT_ID`) + `endpointOrigin` (precedence: `T3CODE_ZEROPS_PUBLIC_ORIGIN` env → the linking request's `Origin`/`X-Forwarded-*`/`Host` → refuse; always `https://`, never loopback). Known gap: `cloudLinkProofHandler`'s pre-existing loopback-only gate on the old `endpoint`/`origin` fields may reject a request forwarded by nginx before the new logic runs — to check when a real trigger exists.                                                                                                                                                                                                                                                                       | `apps/server/src/cloud/http.ts` + tests                                                      |
| **Clients off the T3 Connect reach API**: `client-runtime` `managedRelay.ts` keeps mobile registration + snapshot + linking (`clerkToken` → `zeropsToken`), `discovery.ts` deleted, `RelayConnectionTarget` resolves to `ConnectionBlockedError{reason:"unsupported"}`; web lost the whole managed-tunnel/relay-discovery vertical (connect list, onboarding dialog, install dialog, T3 Connect/mobile-clients profile pages); `cloud/linkEnvironment.ts` survives with zero callers (a Settings → Notifications trigger is the natural home); mobile lost the cloud environment list + Connect onboarding sheet, keeps pairing + Clerk until S5-3, and passes `zeropsToken` from the Clerk session for now. Leftover: `ClientCapabilities.CloudSession` has no reader inside client-runtime. | `git log main -- packages/client-runtime apps/web/src/cloud apps/mobile/src/features/cloud`  |
| **`infra/relay/zerops.yml` + `zerops-import.yml` validate against the platform schema** (zcp `ValidateZeropsYAML` / `ValidateImportYAML`: 0 errors) after fixing the type enums — `ubuntu/nodejs@22` (OS prefix mandatory), `postgresql:single@16` (mode in the type), `PORT` quoted, `RELAY_ISSUER` as a placeholder (`${zeropsSubdomain}` cannot resolve at import time). Deployment to a `z3-relay` project is NOT done — a lasting resource, the owner's go. Side finding: `../zerops-docs/` and ~30 of zcp's `internal/knowledge/recipes/*.import.yml` use the bare `nodejs@22` form the validator rejects.                                                                                                                                                                              | throwaway Go test against `internal/schema`                                                  |
| Git merge silently DUPLICATED four contract exports when two branches added the same block at different anchors (S7 web vs server `ZeropsAgentAuth*` in `zerops.ts`) — no conflict markers. Grep for duplicate `export const` after any "clean" merge of parallel contract work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | S7 web reconcile                                                                             |

### S5-1 — desktop as a pure hosted client (laptop, 2026-08-29)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | How it was established                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| **The desktop app no longer spawns anything but Electron**: `apps/desktop/src/{backend,wsl,ssh,telemetry}/**`, `DesktopClerk.ts`, `packages/ssh/**` and the matching IPC methods/schemas (`getLocalEnvironmentBootstraps/BearerToken`, `wsl*`, `ssh*`, `serverExposure*`, `pickFolder`, `pickProjectFavicon`) are deleted; kept: keychain-backed connection catalog, theme file picker (returns content, not paths), remote-editor probing, saved-environment SSH target TYPES (shared with client-runtime's catalog). `ElectronProtocol` serves the staged hosted-static bundle from `resources/web` (SPA fallback, traversal guard, MIME) in production and proxies to Vite in development; the window opens unconditionally.                                    | `git log main -- apps/desktop`; typecheck desktop/contracts/web/client-runtime/server; desktop 417 tests |
| **Build**: `scripts/build-desktop-artifact.ts` stages `apps/web` through `stageHostedWebBundle` from `scripts/stage-desktop-web.ts` (which owns the channel predicate `resolveDesktopUpdateChannel` since the desktop smoke shares the staging), built with `VITE_HOSTED_APP_CHANNEL=<resolveDesktopUpdateChannel(appVersion)>` (`latest`/`nightly` — the only values the client accepts) and empty `VITE_HTTP_URL`/`VITE_WS_URL` into `extraResources/web` on every platform; the Windows `server.asar` sidecar, the WSL runtime archive and the Clerk-passkey entitlements are gone; the updater defaults to `krls2020/z3`. A full unsigned DMG was not produced on this laptop (`cargo`/`rustc` missing for the untouched `stageResourceMonitor` native build). | script + `build-desktop-artifact.test.ts` (223 tests)                                                    |
| **Proof**: the real Electron binary launched with `--remote-debugging-port` rendered `t3code://app/` with the hosted-static landing ("Zerops Code · Sign in to Zerops · Email / Password · Create one · Connect a backend manually") through the serve-from-disk handler — `readPrimaryEnvironmentTarget()` is never reached because `isHostedStaticApp()` short-circuits `__root.tsx`'s gate.                                                                                                                                                                                                                                                                                                                                                                     | CDP DOM read of the launched app                                                                         |
| **Latent bug removed**: `apps/web/src/main.tsx` still rendered an Electron-specific `ClerkProvider` bound to the deleted preload Clerk bridge (inert while its web public config was unset) — now always the plain provider. Also removed with the desktop merge: the advertised-endpoint/QR multi-endpoint picker in `ConnectionsSettings.tsx` (unfed after the backend deletion; the single `shareablePairingUrl` QR stays), `ConnectionsSettings.logic.ts`, the `defaultAdvertisedEndpointKey` ui-state slice, the SSH password prompt dialog, the per-environment provider-update popover.                                                                                                                                                                     | commits `114166cae`, `529f704b9`, the merge `607c99309`                                                  |
| Open (product positioning, not S5-1): `docs/user/remote-access.md` / `install.md` remain upstream-T3-flavoured (`npx t3@latest`, T3 Connect, installers) beyond the deleted sections.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | read-through                                                                                             |

### S7 follow-up — self-verified agent auth + server-driven login sessions (laptop, 2026-08-29 afternoon)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | How it was established                                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------- | --------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| **The agent-auth feed now verifies itself**: `ZeropsAgentAuthVerify.ts` runs `claude auth status` (JSON `loggedIn`) / `codex login status` (text) through an argv spawner; any spawn failure collapses to `unknown`; `mark-oauth` is gated on this verification, the provider registry refresh is only a best-effort picker-cache warm. `markedOAuth` resets when `ZCP_AGENT_OAUTH_<S>` disappears from the env store; the watcher targets the credential FILE (parent dir filtered by basename) so a probe's writes to `~/.claude/backups`/`sessions` no longer echo; verification results and `mark-oauth` spawns (`changed`, `migrated`) are logged. `ZeropsAgentAuth.ts` reaches the provider registry only through `spi/providerInstances.ts` — zone rule 2 green again. | `ZeropsAgentAuthVerify.test.ts` (21), `ZeropsAgentAuthIo.test.ts`, `ZeropsAgentAuthWatcher.test.ts`, `scripts/z3-zone-architecture.test.ts` 4/4 |
| **Login is server-driven**: `zerops.agentLogin.start {agentId, threadId}` opens an `agent-login-<agent>` terminal, writes the command, feeds the PTY bytes to a near-verbatim port of the Zerops GUI walker's pure output parser (`zeropsAgentLoginOutputParser.ts`: chunk-boundary-safe URL anchors, OSC 8, DEC graphics; 16 ported tests) and handler table trimmed to claude-code/codex, with the walker's stall timer pressing Enter through any unrecognized screen (Claude's login-method menu included); the feed's `login` field carries `phase` (`starting                                                                                                                                                                                                           | menu                                                                                                                                            | awaiting-browser | awaiting-code | succeeded | failed | cancelled`), `url`, `code`, `message`, `terminalId`; success triggers `ZeropsAgentAuth.recheckNow`; one session per agent; `zerops.agentLogin.cancel` sends Ctrl-C and closes. The card renders "Open sign-in link" + "Copy link" (+ "Copy code" for Codex) and no longer types into a client terminal. The raw PTY stream carries no newline at the soft-wrap point — the wrapping seen in F8 was the client terminal's rendering. | `ZeropsAgentLogin.test.ts` (9), `zeropsAgentLoginWalker.test.ts` (17) fixtured on the F8 ledger lines; web card tests |
| Not proven offline: Codex's exact success/error lines beyond `Logged in using <method>` / `Not logged in` (ported from the GUI walker), which stream Codex writes to (both are read), and the Claude menu text (the walker never matches it — it Enters on stall). The `ws.ts` feed handler re-zips two services with `Stream.merge` + re-read instead of `zipLatestWith` to stay clear of this Effect build's debounce/interrupt scheduler bug.                                                                                                                                                                                                                                                                                                                              | code + the S7 fix report                                                                                                                        |

### S7 second live pass on `z3-eval` (`t3@0.1.0-dev.97ea0e72b`, 2026-08-29 13:39–13:47 UTC)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | How it was established                                                                 |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| **F8 works for both agents**: "Sign in to Codex" → feed `login` `starting` → `menu` → `awaiting-browser` with `url https://auth.openai.com/codex/device` + a one-time code 1.06 s after the click (CLI pid ≤0.95 s); the card renders "Open sign-in link · Copy link · Copy code <code>"; `zerops.agentLogin.cancel` → feed `cancelled` in 15–28 ms, process gone ≤0.2 s. "Sign in to Claude" → `menu` → the walker's stall-timer Enter → `awaiting-browser` with a single 450-char `https://claude.com/cai/oauth/authorize?…` string (no wrap) 4.65 s after the click; cancel → `cancelled`, the CLI outlives the terminal ~1.4 s (re-parented to pid 1) then exits. | door WS `subscribeZeropsAgentAuth` + puppeteer on the public URL + 200 ms `ps` sampler |
| **F4 fixed**: exactly one verification log line per restore; `claude -p` writing `~/.claude/backups` and `~/.claude/sessions` no longer triggers a check (count stayed at 1 over 25 s). Restore → feed 397 ms; restore → verification log 2.0–2.1 s (400 ms + 1 000 ms debounce + 0.6–0.7 s `claude auth status`). `mark-oauth` was spawned once at unit boot (`changed:false migrated:false`); the latch held all session because the flag never left the env store (by design).                                                                                                                                                                                     | `journalctl -u zerops@z3`                                                              |
| **F1 still open**: removing the credential flips `credPresent`/`state:reconnect` in 403 ms but runs NO verification — `recomputeCredential` requests the check only when the file exists — so `providerAuth` stays `authenticated` for the whole absent window (94 s observed) while `claude auth status` says `loggedIn:false`. Fix slice in flight: verify on both transitions.                                                                                                                                                                                                                                                                                     | two removal cycles, journal empty for both                                             |
| Feed chattiness: an unchanged snapshot is republished on every PTY chunk while a login is in `menu`/`awaiting-browser` (15 identical chunks in 4.5 s) — `snapshotsEqual` ignores `login`. After a restore the stale `login:{phase:"cancelled"}` rides along (harmless, the card ignores it). Web: the second login session's terminal tab opened unfocused (pane showed an empty shell while the card waited); no Cancel affordance on the card (RPC only); F7 overlap with the terminal pane open persists.                                                                                                                                                          | same run; screenshots `s7b-03…08` in the puppeteer store                               |
| **F1 fixed** (`t3@0.1.0-dev.9abe5c58e`, 14:07 UTC): a credential removal now runs the verification too — `mv ~/.claude/.credentials.json` aside → journal `verification { agentId:'claude-code', providerAuth:'unauthenticated' }` 2.6 s later; `mv` back → `providerAuth:'authenticated'` 4 s after that; `claude -p` → `ok`; no `mark-oauth` on either (the boot spawn at 14:07:54 was `changed:false`). Also in the same build: login-aware `snapshotsEqual` (no republish per PTY chunk), the login terminal tab is focused on start, and the card has Cancel during a session.                                                                                   | ssh + `journalctl -u zerops@z3` on `z3-eval`                                           |

## Home + intake trial — 2026-08-30

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Evidence                                                                                         |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **The fork's home is `zeropsio/z3`** (public, MIT-clean: upstream ships MIT and the notice is retained). `main` + the three own tags (`upstream-base-2026-08-28`, `main-poc-2026-08-28`, `poc-2026-08-28`) pushed; upstream's ~1200 nightly tags deliberately left behind. Remotes: `origin` = `zeropsio/z3`, `personal` = `krls2020/z3`, `upstream` = `pingdotgg/t3code`. Two auth traps: the fine-grained PAT is refused for any push touching `.github/workflows` (no `workflow` permission) — pushed over SSH instead; and Xcode's global `osxkeychain` helper shadows `GIT_ASKPASS` for github.com, so a token push needs `-c credential.helper=`.       | `git push` + `api.github.com/repos/zeropsio/z3/{branches,tags}`                                  |
| Pre-push secret sweep of all 247 own commits (added lines) found no tokens, keys or live passwords; the only `VSCODE_PASSWORD`/`password` hits are obvious test fixtures. Live identifiers that DO ship publicly: the `z3-eval` project/service IDs throughout this ledger. No blob over 50 MB; pack is 170 MiB.                                                                                                                                                                                                                                                                                                                                              | `git log -p upstream-base..main` + pattern sweep                                                 |
| **Intake trial (not a real intake — the last-reviewed SHA was NOT moved).** Upstream drift at `2daff8c25` is 42 commits over 2 days. Zone split: 1 imported (`702a6ade3`, effect-codex-app-server), 2 ported-zone (`c131f2892`, `cb49e5d72`), 1 squarely in the deleted T3 Connect zone (`94f194816`), the rest owned-core/web.                                                                                                                                                                                                                                                                                                                               | `git log f94a0d646..upstream/main` + per-commit path classification                              |
| **Import step works as designed.** `git checkout upstream/main -- packages/effect-{codex-app-server,acp}` changed 2 files; `imported-lock.ts --check` then failed with the exact drifted path (`expected b696268…, got 9a2a0b9…`), `--write --upstream upstream/main` regenerated, re-check green. The re-imported package typechecks (only `suggestion` diagnostics repo-wide) and passes 27/27 of its own tests. Hazard noted: the pre-commit `vp fmt` hook runs on imported files — it changed nothing this time, but a formatting divergence from upstream would show up as lock drift rather than silent corruption.                                     | `node scripts/imported-lock.ts`, `vp test run packages/effect-codex-app-server`, `vpr typecheck` |
| **The SPI earned its keep on the first trial port.** `c131f2892` ("stop querying Claude context usage after turns") cherry-picked with ZERO conflicts, 2 files, and passed the zone-architecture test — then broke 3 of the Claude replay goldens: `turn-abort-error` emits 15 events where the golden has 14, and `zerops-workflow-envelope` / `user-input-requested` diverge at the first `thread.token-usage.updated`. A clean-compiling cherry-pick silently reshaped the lifecycle event stream — exactly the failure `spi.md` was built to catch. Porting it means a deliberate decision plus a golden re-record, not a merge.                          | `git cherry-pick -n c131f2892`, `vp test run apps/server/src/spi` (3 failed / 189 passed)        |
| **Port conflicts land on the SPI seam, and are legible.** `cb49e5d72` (opencode, 42 files) conflicted in 3: `OpenCodeTextGeneration.ts` twice over upstream importing `OpenCodeRuntime`/`OpenCodeServerOwner` directly where we go through `openCodeRuntimeCapability` (zone rule 2 working), and `server.test.ts` purely because our zerops websocket test sits adjacent. A deleted-zone commit is correspondingly expensive: `94f194816` (fix(connect), DPoP) conflicted in 9 across `cloud/linkEnvironment`, `client-runtime/relay` and `infra/relay` — the triage is right to skip it by moving the SHA. `git rerere` is on and recorded the resolutions. | `git cherry-pick -n` on each; conflict counts from `git diff --diff-filter=U`                    |
| Trial artefacts: branch `trial/intake-2026-08-30` (local only, `089fd5632`) holds the import + the c131f2892 port. `main` was left untouched at `bb0b64a09` and its lock check is green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `git log`, `imported-lock.ts --check`                                                            |

## First CI run ever, on `zeropsio/z3` — 2026-08-30

Actions had never run on this fork (the personal mirror had it off). The first run on `main` `bb0b64a09` is RED in 4 jobs. All four are pre-existing on `main`; none was introduced by the move.

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Evidence                                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| **Check / `vp fmt`**: `apps/server/src/spi/fixtures/claude/turn-abort-error.meta.json` is unformatted. Reproduces locally.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `vp fmt --check <file>`                                                               |
| **Test / `packages/contracts/src/providerRuntimeSpi.test.ts`**: `expected '2.1' to be '2.0'` — the SPI version bump to 2.1 (SPI-2, documented in `spi.md`) never updated its own guard test. Reproduces locally.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `vp test run packages/contracts/src/providerRuntimeSpi.test.ts`                       |
| **Release Smoke**: `ENOENT … packages/ssh/package.json`. `packages/ssh` was deleted from git at the freeze (desktop SSH-launch removal, checklist 4) but `scripts/release-smoke.ts:29` still lists it. Invisible on the dev laptop because the directory survives there UNTRACKED — `git ls-files packages/ssh` is empty while `ls packages/ssh` succeeds. Only a clean checkout can see it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `git ls-files packages/ssh` (0), `git ls-tree origin/main packages/` (no ssh), CI log |
| **The SPI replay goldens are coupled to the developer's macOS tmpdir — they cannot pass on any Linux machine, the Zerops container included.** `goldens.test.ts` builds `REDACT_PATHS` from the CURRENT process (`process.cwd()`, `os.homedir()`, `os.tmpdir()`) and applies it to RECORDED content. The Claude fixtures' `session.configured` carries the recording container's `"messaging_socket_path": "/tmp/cc-socks/164264.sock"`. Where `os.tmpdir()` is `/var/folders/…` (macOS) that string survives literally and matches the golden; where it is `/tmp` (every GitHub runner, and the container) it is rewritten to `<TMPDIR>/cc-socks/…` and all four Claude goldens diverge at event index 5. Proven by `TMPDIR=/tmp vp test run apps/server/src/spi/replay/goldens.test.ts` — 10/10 green becomes 4 failed, byte-identical to CI. The regression gate the whole fork leans on is therefore green only on one laptop. | local run with and without `TMPDIR=/tmp`; CI job 99230261257                          |
| Corollary: `describeFirstDivergence` reports only the event index and type (`actual=session.configured expected=session.configured`), never the differing field — a CI-only golden failure gives the reader nothing to act on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | same failure text in both environments                                                |
| **Test Server 2 / `server.test.ts`**: 3 `cloud link proofs` cases assert `'Invalid managed endpoint origin.'` but get `'This environment is not running on Zerops…'` — the code short-circuits off-platform, so the assertion is unreachable anywhere but a Zerops container. Reproduces locally.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `vp test run apps/server/src/server.test.ts -t "cloud link proofs"`                   |
| **`krls2020/z3` archived (read-only) 2026-08-30; `zeropsio/z3` is the single link.** The `personal` remote is gone. Every live pointer repointed in the same change: `ModelManifest.ts` (runtime manifest fetch), `build-desktop-artifact.ts` (updater default repo) + its test, and `infra/relay/zerops-import.yml` (`buildFromGit`) — this supersedes the `krls2020/z3` values recorded in the day-1 rows above. The new manifest URL answers 200; `build-desktop-artifact.test.ts` 28/28 and `ModelManifest.test.ts` 7/7 green.                                                                                                                                                                                                                                                                                                                                                                                                 | `curl` on raw.githubusercontent + both test runs                                      |

## Turning the first CI run green — 2026-08-30

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Evidence                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| **The SPI goldens were green on exactly one laptop, and the cause was dead code.** `redact()` rewrote any string equal to (or under) the RUNNING host's `cwd`/`homedir`/`tmpdir`, applied to fixture content recorded elsewhere. The Claude recordings carry the container's `"/tmp/cc-socks/164264.sock"`: on macOS (`tmpdir` `/var/folders/…`) it survived and matched; on any Linux host (`tmpdir` `/tmp`) it became `<TMPDIR>/…` and all four Claude goldens diverged at event index 5. Measured: across all EIGHT goldens there is not one `<CWD>`/`<HOME>`/`<TMPDIR>` placeholder — the path facility never masked a single character, it could only misfire. Removed whole (interface, implementation, its four tests, the one caller); every golden stayed byte-identical, and the suite passes on macOS and under `TMPDIR=/tmp` alike. A regression test now pins that redaction never consults the environment. Rule going forward: if a recording needs a path masked, mask it when RECORDING, never when comparing. | `TMPDIR=/tmp vp test run apps/server/src/spi/replay/` before (4 failed) and after (all green); `git status` on the fixtures = 0 |
| Nothing in the replay suite needs a live agent — that was checked, not assumed. Claude/Codex replay a static `.jsonl`; Cursor/Grok drive the real adapter against the scripted `scripts/acp-mock-agent.ts`; OpenCode uses an `OpenCodeRuntime` test double. That is why all three non-Claude baselines passed on a GitHub runner with no CLI installed. The step that DOES need a real agent — `spi/recording/record-claude.mjs` — is not in CI and must not be.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | CI job 99230261257 (10 tests, only the 4 Claude ones red) + the three replay modules                                            |
| **Three `cloud link proofs` tests asserted a branch no machine could reach.** `cloudLinkProofHandler` gates on `config.zerops` BEFORE it validates the endpoint origin (added by `0ba0597ad`, project-bound proofs), so an app built with no Zerops environment always answered `"This environment is not running on Zerops…"` and the origin rules the tests are named after went untested everywhere, laptop included. Reaching them needs the whole chain the handler demands: a Zerops environment, a resolvable public origin (`resolveZeropsLinkProofOrigin` gates next), and a bearer token — the Zerops door refuses cookie sessions by design (`browser_session_unsupported`), so the cookie helper these tests used cannot work there. All six cases green.                                                                                                                                                                                                                                                           | `vp test run apps/server/src/server.test.ts` 147/147                                                                            |
| **`vp check` exits 1 on `main` — 11 `t3code(namespace-node-imports)` errors** in `ZeropsAgentAuthWatcher.{ts,test.ts}`, `ElectronProtocol.test.ts`, `record-claude.mjs`, `infra/relay/scripts/migrate.ts`. Pre-existing and identical with the day's work stashed; nobody had ever run the repo-wide check. Renamed to the `NodeX` namespace form (each rewrite asserting its own match count, since there is no AST tooling; the four prose references to Node's `fs.watch` API were left alone). `vp check` now exits 0 with 0 errors / 23 warnings.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `vp check` exit code before (1) and after (0)                                                                                   |
| Day's tally on the first-ever CI run: **five independent pre-existing failures**, none introduced by the move, none visible on a dev laptop — unformatted fixtures, a stale SPI-version guard (2.0 vs the 2.1 the constant and `spi.md` both declare), a `release-smoke.ts` entry for the `packages/ssh` deleted at the freeze (still present untracked on the laptop), the tmpdir-coupled goldens, and the 11 lint errors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | runs 33301432278 → 33305760166                                                                                                  |
| **The preload verifier still required what the freeze deleted.** `b36e895b3` made the desktop a pure hosted client, removing `getLocalEnvironmentBootstraps`, `pickFolder` and the Clerk passkeys bridge from `preload.ts` AND from the `DesktopBridge` contract — but never touched `verify-preload-bundle.mjs`, which demanded all three. Note `pickFolder` still exists in `ElectronDialog.ts` (the app-menu dialog service), so source presence alone would have read as a real bug; the contract settled it. The list now names four APIs the bridge genuinely exposes, so the script still proves the bundle executes and yields a working bridge — mutation-checked against an API that is absent.                                                                                                                                                                                                                                                                                                                       | `git show b36e895b3 -- apps/desktop/src/preload.ts`; `vp run build:desktop` + the verifier, exit 0                              |
| Self-inflicted, recorded because the lesson is the point: narrowing that list broke `verify-preload-bundle.test.mjs`, whose synthetic preload still built the deleted bridge — the same follow-the-chain miss, one commit after fixing it. Cause was the verification, not the edit: `vp check` + typecheck + running the verifier itself, but not the package's tests. The rule that catches it is to run CI's own commands — `vp run --parallel --filter '!t3' --filter '!@t3tools/monorepo' test` and `vp run --filter t3 test`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 4 red fixtures, then 9/9                                                                                                        |
| **`entrypoint.test.ts`'s symlink case has been red on every Mac and green on every Linux — the SPI-golden disease with the platforms swapped.** `makeTempDir` returned `os.tmpdir()` unresolved; macOS reaches its temp dir through `/var -> /private/var`, so the fixture's `moduleUrl` was `/var/…` while `isEntrypoint` correctly compared it to `realpathSync`'s `/private/var/…`. Linux has no such link (`/tmp`), so CI never saw it and only macOS developers did — meaning the whole `apps/server` suite had never been green on a laptop. Production code is right (a running module's URL IS the real path, as its comment says); the fixture now realpaths. Green with and without `TMPDIR=/tmp`, and pre-existing (reproduced with the day's work stashed).                                                                                                                                                                                                                                                         | `vp run --filter t3 test` before (1 failed / 3405) and after                                                                    |
| **CI is green on `07c3d0d8c`**: 8 jobs green, `Mobile Native Static Analysis` skipped by its own `needs.mobile_native_changes` guard (no native changes), run conclusion `success`. Freeze checklist item 3's open question — GitHub-hosted runners — is answered: the whole suite runs on them, and the seven failures it surfaced were all pre-existing, none of them visible on a dev laptop.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | run 33306... jobs API                                                                                                           |
