{
  "driver": "claude",
  "cliVersion": "2.1.251 (Claude Code)",
  "sdkVersion": "0.3.250",
  "model": "claude-opus-5[1m]",
  "capturedAt": "2026-08-29T08:23:33.798Z",
  "capturedOn": "z3-eval/zcp",
  "capturedBy": "spi-3",
  "cwd": "/var/www",
  "prompt": "Use the AskUserQuestion tool to ask me a single question with header \"Color\" and exactly two options: Blue and Green. After you get my answer, reply with the single word done and stop.",
  "allowedTools": [
    "mcp__zerops__zerops_workflow",
    "mcp__zerops__zerops_mount",
    "mcp__zerops__zerops_discover"
  ],
  "notes": "Confirms AskUserQuestion routes through canUseTool regardless of permissionMode=auto (matches the adapter's comment at ClaudeAdapter.ts ~4108: handled 'regardless of runtime mode'). One control line: canUseTool with toolName=AskUserQuestion, input.questions[0]= {question:'Which color do you prefer?', options:[Blue,Green]}; --answer=first selected 'Blue', returned as {behavior:allow, updatedInput:{questions, answers:{<question text>: 'Blue'}}} -- the exact shape ClaudeAdapter.ts's handleAskUserQuestion expects back (answer keyed by full question text, not index/id). Turn continued normally to a final assistant message \"done\" and result:success. onUserDialog was never invoked (no resume in this fresh session).",
  "sdkOptions": {
    "cwd": "/var/www",
    "pathToClaudeCodeExecutable": "~/.local/bin/claude",
    "systemPrompt": {
      "type": "preset",
      "preset": "claude_code"
    },
    "settingSources": ["user", "project", "local"],
    "permissionMode": "auto",
    "includePartialMessages": true,
    "canUseTool": "<function>",
    "onUserDialog": "<function>",
    "supportedDialogKinds": ["resume_return"],
    "env": "<omitted>",
    "additionalDirectories": ["/var/www"]
  }
}
